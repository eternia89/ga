---
status: resolved
phase: 09.1-ui-improvements
source: [09.1-01-SUMMARY.md, 09.1-02-SUMMARY.md, 09.1-03-SUMMARY.md]
started: 2026-02-27T07:15:00Z
updated: 2026-02-27T12:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Request list — CTA position and compact columns
expected: Navigate to /requests. "New Request" button is right-aligned beside page title. Category column is removed. Status badge is beside the ID in the same cell.
result: issue
reported: "it should be implemented to all pages that share the same layout, not just request page"
severity: major

### 2. Request list — ghost action buttons
expected: On /requests, each row should show small visible icon buttons (eye, clipboard, etc.) instead of a three-dot dropdown menu. Hovering over buttons should show tooltip titles.
result: issue
reported: "use text instead of icon to make user didn't have to guess"
severity: minor

### 3. Job list — CTA position and columns
expected: Navigate to /jobs (as GA Lead or Admin). "New Job" button is right-aligned beside page title. Status badge is beside the JOB-YYYY-NNNN ID in the same cell.
result: pass

### 4. Job list — ghost buttons and linked requests
expected: On /jobs, each row has visible ghost icon buttons instead of dropdown. Linked request chips are stacked vertically (one per line) instead of truncated with "+N more".
result: issue
reported: "ghost icon should be changed to text so users doesn't need to guess what these icons means"
severity: minor

### 5. Pagination defaults
expected: On any list page (requests, jobs, assets, etc.), the table defaults to 50 rows per page. The page size dropdown shows options: 50, 100, 200.
result: pass

### 6. Excel export — human-readable names
expected: On a list page, click the Excel export button and download the file. Open it — columns should show location names, category names, requester names, PIC names (not raw UUID strings).
result: issue
reported: "on any CTA button, it should be placed beside the page title. in this case, export should be placed to the left of 'new request' button"
severity: major

### 7. Date range picker — jobs
expected: On /jobs, the date filter should be a single date range picker (click to open calendar popup, select start and end date). Not two separate date input fields. Dates display as dd-MM-yyyy.
result: pass

### 8. Date range picker — requests
expected: On /requests, same date range picker pattern as jobs — single calendar popup for selecting a date range.
result: pass

### 9. Users list — compact layout
expected: Navigate to /admin/settings (Users tab). No avatar/initials column. Email appears as smaller muted text below the name in the same cell. Last login shows date only (dd-MM-yyyy, no time). Row actions are ghost icon buttons (Edit, Deactivate) instead of dropdown.
result: pass

### 10. User detail permalink
expected: Navigate to /admin/settings?tab=users&userid={any-valid-user-id}. The user edit modal should auto-open for that user. The URL should be shareable — copying and pasting it in a new tab opens the same modal.
result: pass

## Summary

total: 10
passed: 6
issues: 4
pending: 0
skipped: 0

## Gaps

- truth: "CTA button positioning and compact column layout applied consistently to all list pages"
  status: resolved
  reason: "User reported: it should be implemented to all pages that share the same layout, not just request page"
  severity: major
  test: 1
  root_cause: "CTA buttons only moved to page headers for requests/jobs. Assets, templates, schedules, and admin settings tabs still have createButton in DataTable toolbar or missing entirely."
  artifacts:
    - path: "app/(dashboard)/inventory/page.tsx"
      issue: "No New Asset button in page header"
    - path: "app/(dashboard)/maintenance/templates/page.tsx"
      issue: "No New Template button in page header (button is inside TemplateList component)"
    - path: "app/(dashboard)/maintenance/page.tsx"
      issue: "No New Schedule button in page header (button is inside ScheduleList component)"
    - path: "components/admin/companies/company-table.tsx"
      issue: "createButton passed to DataTable toolbar"
    - path: "components/admin/divisions/division-table.tsx"
      issue: "createButton passed to DataTable toolbar"
    - path: "components/admin/locations/location-table.tsx"
      issue: "createButton passed to DataTable toolbar"
    - path: "components/admin/categories/category-table.tsx"
      issue: "createButton passed to DataTable toolbar"
    - path: "components/admin/users/user-table.tsx"
      issue: "createButton passed to DataTable toolbar"
  missing:
    - "Move CTA buttons to page headers on all list pages"
    - "Remove createButton prop from DataTable toolbar calls"
  debug_session: ""
- truth: "Row action buttons use text labels so users don't have to guess what icons mean"
  status: resolved
  reason: "User reported: use text instead of icon to make user didn't have to guess"
  severity: minor
  test: 2
  root_cause: "request-columns.tsx uses variant='ghost' size='icon' with Lucide icon children only (Eye, ClipboardList, CheckCircle, XCircle, Ban). No text labels."
  artifacts:
    - path: "components/requests/request-columns.tsx"
      issue: "6 icon-only ghost buttons for row actions"
  missing:
    - "Change to variant='ghost' size='sm' with text labels"
  debug_session: ""
- truth: "Job list row action buttons use text labels instead of icons"
  status: resolved
  reason: "User reported: ghost icon should be changed to text so users doesn't need to guess what these icons means"
  severity: minor
  test: 4
  root_cause: "job-columns.tsx uses variant='ghost' size='icon' with Lucide icon children only (Eye, Pencil, XCircle). No text labels. user-columns.tsx has same pattern (Pencil, CheckCircle, Ban)."
  artifacts:
    - path: "components/jobs/job-columns.tsx"
      issue: "3 icon-only ghost buttons for row actions"
    - path: "components/admin/users/user-columns.tsx"
      issue: "3 icon-only ghost buttons for row actions"
  missing:
    - "Change to variant='ghost' size='sm' with text labels on all column files"
  debug_session: ""
- truth: "Export button placed beside page title (left of New Request/New Job CTA), not inside table toolbar"
  status: resolved
  reason: "User reported: on any CTA button, it should be placed beside the page title. in this case, export should be placed to the left of 'new request' button"
  severity: major
  test: 6
  root_cause: "Export button rendered inside DataTableToolbar (data-table-toolbar.tsx lines 224-245) via exportUrl prop. Should be in page header instead."
  artifacts:
    - path: "components/data-table/data-table-toolbar.tsx"
      issue: "Export button rendered in toolbar right side via exportUrl prop"
  missing:
    - "Move export button to page headers on all list pages with export"
    - "Remove exportUrl rendering from DataTableToolbar"
  debug_session: ""
