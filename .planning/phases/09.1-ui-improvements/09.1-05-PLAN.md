---
phase: 09.1-ui-improvements
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - components/requests/request-columns.tsx
  - components/jobs/job-columns.tsx
  - components/admin/users/user-columns.tsx
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - "Request list row action buttons show text labels (View, Triage, Accept, Reject, Cancel) instead of icon-only buttons"
    - "Job list row action buttons show text labels (View, Edit, Cancel) instead of icon-only buttons"
    - "User list row action buttons show text labels (Edit, Deactivate, Reactivate) instead of icon-only buttons"
  artifacts:
    - path: "components/requests/request-columns.tsx"
      provides: "Text-labeled ghost buttons for request row actions"
    - path: "components/jobs/job-columns.tsx"
      provides: "Text-labeled ghost buttons for job row actions"
    - path: "components/admin/users/user-columns.tsx"
      provides: "Text-labeled ghost buttons for user row actions"
  key_links:
    - from: "components/requests/request-columns.tsx"
      to: "Button component"
      via: "variant=ghost size=sm with text children"
      pattern: "size=\"sm\".*View"
---

<objective>
Change all icon-only ghost action buttons in table rows to text-labeled ghost buttons so users don't have to guess what each icon means.

Purpose: UAT gap closure — user reported "use text instead of icon to make user didn't have to guess" on both request and job list pages. User-columns also has the same icon-only pattern.
Output: All three column files use `variant="ghost" size="sm"` buttons with text labels instead of `variant="ghost" size="icon"` with icon-only children.
</objective>

<execution_context>
@/Users/melfice/.claude/get-shit-done/workflows/execute-plan.md
@/Users/melfice/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09.1-ui-improvements/09.1-UAT.md
@components/requests/request-columns.tsx
@components/jobs/job-columns.tsx
@components/admin/users/user-columns.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace icon-only buttons with text-labeled buttons in request, job, and user column files</name>
  <files>
    components/requests/request-columns.tsx
    components/jobs/job-columns.tsx
    components/admin/users/user-columns.tsx
  </files>
  <action>
    **Pattern change for all three files:**
    Change every row action button from:
    ```tsx
    <Button variant="ghost" size="icon" className="h-7 w-7" title="Action Name" ...>
      <IconComponent className="h-3.5 w-3.5" />
    </Button>
    ```
    To:
    ```tsx
    <Button variant="ghost" size="sm" className="h-7 px-2 text-xs" ...>
      Action Name
    </Button>
    ```
    - Remove the `title` attribute (no longer needed since the text IS the label).
    - Remove the icon imports (`Eye`, `ClipboardList`, `XCircle`, `Ban`, `CheckCircle`, `Pencil`) if they are no longer used anywhere in the file. Keep `ImageIcon` in request-columns.tsx since it's used for the photo column, not actions.
    - Use `text-xs` for compact text that fits the table row height.
    - For destructive actions, keep the existing color classes: `text-destructive hover:text-destructive`.
    - For the accept work button, keep `text-green-600 hover:text-green-700`.

    **request-columns.tsx** — Replace 6 action buttons:
    1. View Details (Eye icon) → "View"
    2. Triage (ClipboardList icon) → "Triage"
    3. Accept Work (CheckCircle icon, green) → "Accept"
    4. Reject Work (XCircle icon, destructive) → "Reject"
    5. Reject (XCircle icon, destructive) → "Reject"
    6. Cancel (Ban icon, destructive) → "Cancel"

    Note: "Reject Work" and "Reject" use the same text "Reject" but they are conditionally exclusive (one shows for pending_acceptance, the other for submitted/triaged), so no ambiguity.

    After changes, remove unused icon imports: `Eye`, `ClipboardList`, `XCircle`, `Ban`, `CheckCircle`. Keep `ImageIcon` (used in photo column).

    Widen the actions column size from 120 to 180 to accommodate text labels: `size: 180`.

    **job-columns.tsx** — Replace 3 action buttons:
    1. View Details (Eye icon) → "View"
    2. Edit (Pencil icon) → "Edit"
    3. Cancel (XCircle icon, destructive) → "Cancel"

    After changes, remove unused icon imports: `Eye`, `Pencil`, `XCircle`.

    Widen the actions column size from 80 to 150: `size: 150`.

    **user-columns.tsx** — Replace 3 action buttons in the `UserActions` component:
    1. Edit user (Pencil icon) → "Edit"
    2. Reactivate user (CheckCircle icon, green) → "Reactivate"
    3. Deactivate user (Ban icon, destructive) → "Deactivate"

    After changes, remove unused icon imports: `Pencil`, `Ban`, `CheckCircle`.

    No column size change needed since user actions are in a named `actions` column that auto-sizes.
  </action>
  <verify>
    <automated>cd /Users/melfice/code/ga && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Visit /requests, /jobs, /admin/settings (Users tab) — each row should show text buttons (View, Edit, Triage, Cancel, etc.) instead of ambiguous icons.</manual>
  </verify>
  <done>All three column files use text-labeled ghost buttons for row actions. No icon-only action buttons remain. Users can read the action name directly without guessing.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. Request list row actions show text labels: View, Triage, Accept, Reject, Cancel
3. Job list row actions show text labels: View, Edit, Cancel
4. User list row actions show text labels: Edit, Deactivate, Reactivate
5. No icon-only ghost buttons remain for row actions in any table
</verification>

<success_criteria>
- Every row action button across all data tables displays a readable text label
- Destructive actions retain their red color styling
- Accept/Reactivate actions retain their green color styling
- TypeScript compilation passes
- Table columns are wide enough to display all action buttons without overflow
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-ui-improvements/09.1-05-SUMMARY.md`
</output>
