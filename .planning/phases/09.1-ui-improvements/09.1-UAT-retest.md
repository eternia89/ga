---
status: complete
phase: 09.1-ui-improvements
source: [09.1-04-SUMMARY.md, 09.1-05-SUMMARY.md]
started: 2026-02-27T12:30:00Z
updated: 2026-02-27T13:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Inventory page — CTA and Export in header
expected: Navigate to /inventory. "New Asset" button appears right-aligned beside the page title (not inside the table). An "Export" button appears to the left of "New Asset". Both are in the page header area, above the data table.
result: pass

### 2. Maintenance pages — Templates and Schedules CTAs in headers
expected: Navigate to /maintenance/templates. "New Template" button appears right-aligned beside the page title. Then navigate to /maintenance (schedules). "New Schedule" button appears right-aligned beside the page title, with "Export" button to its left. Both pages have buttons in page headers, not inside the table.
result: pass

### 3. Requests and Jobs — Export buttons beside CTAs
expected: Navigate to /requests. An "Export" button now appears to the LEFT of the "New Request" button in the page header. Navigate to /jobs. An "Export" button appears to the LEFT of the "New Job" button. The table toolbar area should NOT have any export button.
result: pass

### 4. Admin settings — Create buttons above DataTable
expected: Navigate to /admin/settings. Switch through all 5 tabs (Companies, Divisions, Locations, Categories, Users). Each tab should show a section header with the entity name (e.g., "Companies") and a "Create Company" button to its right, positioned ABOVE the data table. The buttons should NOT be inside the table toolbar row.
result: pass

### 5. Request list — text-labeled row actions
expected: On /requests, each row shows text-labeled action buttons (e.g., "View", "Triage", "Accept", "Reject", "Cancel") instead of small icon-only buttons. The text labels are directly readable without hovering. Destructive actions (Reject, Cancel) appear in red.
result: pass

### 6. Job list — text-labeled row actions
expected: On /jobs, each row shows text-labeled action buttons: "View", "Edit", "Cancel" instead of icon-only buttons. "Cancel" appears in red. No icon-only ghost buttons remain.
result: pass

### 7. User list — text-labeled row actions
expected: On /admin/settings (Users tab), each user row shows text-labeled action buttons: "Edit", "Deactivate" (or "Reactivate" for inactive users) instead of icon-only buttons. "Deactivate" appears in red, "Reactivate" in green.
result: pass

## Summary

total: 7
passed: 7
issues: 0
pending: 0
skipped: 0

## Gaps

[none yet]
