---
phase: 09.1-ui-improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/requests/request-columns.tsx
  - components/jobs/job-columns.tsx
  - app/(dashboard)/requests/page.tsx
  - app/(dashboard)/jobs/page.tsx
  - components/requests/request-table.tsx
  - components/jobs/job-table.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "New Request / New Job button appears to the right of the page title, not inside the data table toolbar"
    - "Request list has no Category column; columns are compact"
    - "Status badge is directly beside the ID column in both request and job list tables"
    - "Row actions are visible ghost icon buttons (Eye, Triage, Cancel, etc.), not hidden behind a three-dot dropdown menu"
    - "Linked requests in job list are displayed as vertically stacked chips, not truncated with +N"
  artifacts:
    - path: "components/requests/request-columns.tsx"
      provides: "Compact request columns with status beside ID, ghost action buttons, no category column"
    - path: "components/jobs/job-columns.tsx"
      provides: "Compact job columns with status beside ID, ghost action buttons, vertically stacked linked requests"
    - path: "app/(dashboard)/requests/page.tsx"
      provides: "CTA button beside page title"
    - path: "app/(dashboard)/jobs/page.tsx"
      provides: "CTA button beside page title"
  key_links:
    - from: "app/(dashboard)/requests/page.tsx"
      to: "components/requests/request-table.tsx"
      via: "createButton prop removed from DataTable, button rendered in page header instead"
    - from: "app/(dashboard)/jobs/page.tsx"
      to: "components/jobs/job-table.tsx"
      via: "createButton prop removed from DataTable, button rendered in page header instead"
---

<objective>
Overhaul list page columns and layout for requests and jobs: move CTA buttons to page header, remove category column from requests, place status beside ID, replace three-dot menus with visible ghost icon buttons, and stack linked request chips vertically in job list.

Purpose: Five backlog items (1, 2, 5, 6, 7) all affect the column definitions and layout of request and job list pages. Grouping them produces a cohesive, compact list page overhaul.

Output: Updated request-columns.tsx, job-columns.tsx, request page, job page, request-table.tsx, job-table.tsx
</objective>

<execution_context>
@/Users/melfice/.claude/get-shit-done/workflows/execute-plan.md
@/Users/melfice/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/BACKLOG-UI-IMPROVEMENTS.md
@components/requests/request-columns.tsx
@components/requests/request-table.tsx
@components/jobs/job-columns.tsx
@components/jobs/job-table.tsx
@app/(dashboard)/requests/page.tsx
@app/(dashboard)/jobs/page.tsx
@components/data-table/data-table-toolbar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move CTA buttons to page headers and overhaul request columns</name>
  <files>
    app/(dashboard)/requests/page.tsx
    app/(dashboard)/jobs/page.tsx
    components/requests/request-table.tsx
    components/jobs/job-table.tsx
    components/requests/request-columns.tsx
  </files>
  <action>
**Backlog items addressed: 1 (CTA position), 2 (compact columns, remove category), 5 (ghost buttons), 6 (status beside ID)**

**1. CTA button to page header (requests page + jobs page):**

In `app/(dashboard)/requests/page.tsx`:
- Change the `<div>` wrapping the `<h1>` and `<p>` to a flex container: `flex items-center justify-between`
- Add the "New Request" button (Plus icon + text) as a Link/Button to the RIGHT of the heading block
- Remove the `createButton` prop from `<RequestTable>` (it will no longer pass it to DataTable)

In `app/(dashboard)/jobs/page.tsx`:
- Same pattern: flex header with h1 left, "New Job" button right (only if GA Lead/Admin — pass `currentUserRole` prop or check role inline)
- Remove `createButton` from `<JobTable>`

In `components/requests/request-table.tsx`:
- Remove the `createButton` prop from the `<DataTable>` call (no longer needed since button is in page header)

In `components/jobs/job-table.tsx`:
- Remove the `createButton` prop from the `<DataTable>` call

**2. Request columns overhaul (`components/requests/request-columns.tsx`):**

a) **Remove Category column entirely** — delete the column definition with `id: 'category_name'`

b) **Move Status badge beside ID** — merge status into the display_id column cell. The cell should render:
```tsx
<div className="flex items-center gap-2">
  <span className="font-mono text-xs">{row.getValue('display_id')}</span>
  <RequestStatusBadge status={row.original.status} />
</div>
```
Remove the standalone `status` column definition.

c) **Compact column sizes** — reduce sizes:
- display_id (with status): size 200 (was 130 for ID alone)
- photo: size 50 (was 56)
- title: size 200 (keep)
- location: size 130 (was 140)
- priority: size 90 (was 100)
- assigned_user: size 120 (keep)
- created_at: size 100 (was 110)

d) **Replace three-dot dropdown with ghost icon buttons** — In the `actions` column cell:
- Remove the entire `<DropdownMenu>` structure
- Replace with a `<div className="flex items-center gap-1">` containing visible ghost `<Button>` elements:
  - Always: `<Button variant="ghost" size="icon" className="h-7 w-7">` with `<Eye className="h-3.5 w-3.5" />` for View (onClick -> meta.onView)
  - If canTriage: button with `<ClipboardList>` icon
  - If canAcceptOrRejectWork: green `<CheckCircle>` button and red `<XCircle>` button
  - If canReject: red `<XCircle>` button
  - If canCancel: red `<Ban>` button
- Use `title` attribute on each button for accessibility (e.g., title="View Details")
- Keep the same role/status logic for showing/hiding action buttons
- Import Tooltip from shadcn if desired, but simple `title` attribute is sufficient
  </action>
  <verify>
    <automated>cd /Users/melfice/code/ga && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Open request list page; verify CTA is in header, no category column, status badge beside ID, ghost buttons visible</manual>
  </verify>
  <done>Request list page has CTA in header, no category column, status beside ID, and visible ghost icon buttons for actions</done>
</task>

<task type="auto">
  <name>Task 2: Overhaul job columns — status beside ID, ghost buttons, vertical linked requests</name>
  <files>
    components/jobs/job-columns.tsx
  </files>
  <action>
**Backlog items addressed: 5 (ghost buttons), 6 (status beside ID), 7 (linked requests vertical stacking)**

**1. Move Status badge beside ID** — merge status into the display_id column cell:
```tsx
<div className="flex items-center gap-2">
  <span className="font-mono text-xs">{row.getValue('display_id')}</span>
  <JobStatusBadge status={row.original.status} />
</div>
```
Remove the standalone `status` column definition. Update display_id column size to ~200.

**2. Linked requests — vertical stacking (item 7):**
Replace the current `linked_request` cell that shows first request + "+N more" with a vertical stack:
```tsx
cell: ({ row }) => {
  const jobRequests = row.original.job_requests;
  if (!jobRequests?.length) {
    return <span className="text-muted-foreground">—</span>;
  }
  return (
    <div className="flex flex-col gap-0.5">
      {jobRequests.map((jr) => (
        <Link
          key={jr.request.id}
          href={`/requests/${jr.request.id}`}
          className="font-mono text-xs hover:underline text-foreground"
          onClick={(e) => e.stopPropagation()}
        >
          {jr.request.display_id}
        </Link>
      ))}
    </div>
  );
},
```
Keep size ~130 and enableSorting: false.

**3. Replace three-dot dropdown with ghost icon buttons:**
- Remove the `<DropdownMenu>` structure in actions column
- Replace with `<div className="flex items-center gap-1">`:
  - Always: ghost button with `<Eye>` icon (View Details)
  - If isGaLeadOrAdmin: ghost button with `<Pencil>` icon (Edit) — note "Edit" navigates to job detail page where editing is inline, NOT a separate edit page
  - If canCancel: ghost button with `<XCircle>` icon in destructive color
- Use `title` attribute for accessibility
- Keep same role logic

**4. Compact column sizes:**
- display_id (with status): 200
- title: 220 (was 240)
- pic_name: 120 (was 130)
- priority: 90 (was 100)
- linked_request: 130 (keep)
- created_at: 100 (was 110)
- actions: 80 (was 60, slightly wider for visible buttons)
  </action>
  <verify>
    <automated>cd /Users/melfice/code/ga && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Open job list page; verify status beside ID, linked requests stacked vertically, ghost buttons visible</manual>
  </verify>
  <done>Job list page has status badge beside ID, vertically stacked linked request chips, and visible ghost icon action buttons</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. Request list page: CTA in page header, no category column, status beside ID, ghost action buttons
3. Job list page: CTA in page header, status beside ID, linked requests stacked, ghost action buttons
4. Both pages: no three-dot dropdown menus remain
</verification>

<success_criteria>
- CTA buttons ("New Request", "New Job") appear right-aligned next to page titles
- Request list has no Category column
- Status badge is merged into the ID cell on both request and job lists
- All row actions are visible ghost icon buttons, not hidden in dropdown menus
- Linked requests in job list display vertically stacked, not truncated
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-ui-improvements/09.1-01-SUMMARY.md`
</output>
