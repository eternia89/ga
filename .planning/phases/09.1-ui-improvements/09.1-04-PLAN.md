---
phase: 09.1-ui-improvements
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(dashboard)/inventory/page.tsx
  - app/(dashboard)/maintenance/templates/page.tsx
  - app/(dashboard)/maintenance/page.tsx
  - app/(dashboard)/requests/page.tsx
  - app/(dashboard)/jobs/page.tsx
  - components/assets/asset-table.tsx
  - components/maintenance/template-list.tsx
  - components/maintenance/schedule-list.tsx
  - components/admin/companies/company-table.tsx
  - components/admin/divisions/division-table.tsx
  - components/admin/locations/location-table.tsx
  - components/admin/categories/category-table.tsx
  - components/admin/users/user-table.tsx
  - components/requests/request-table.tsx
  - components/data-table/data-table-toolbar.tsx
  - components/data-table/data-table.tsx
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - "Every list page (requests, jobs, inventory, templates, schedules) has its CTA button right-aligned beside the page title in the server page header"
    - "Every admin settings tab (companies, divisions, locations, categories, users) has its Create button in the tab content area header, not inside the DataTable toolbar"
    - "Export button appears in page headers beside the CTA button, not inside the DataTable toolbar"
    - "No createButton or exportUrl props are passed to DataTable or DataTableToolbar from any table component"
  artifacts:
    - path: "app/(dashboard)/inventory/page.tsx"
      provides: "New Asset CTA in page header"
    - path: "app/(dashboard)/maintenance/templates/page.tsx"
      provides: "New Template CTA in page header"
    - path: "app/(dashboard)/maintenance/page.tsx"
      provides: "New Schedule CTA in page header"
    - path: "app/(dashboard)/requests/page.tsx"
      provides: "Export button beside New Request CTA in page header"
    - path: "components/data-table/data-table-toolbar.tsx"
      provides: "Cleaned toolbar without createButton or exportUrl rendering"
  key_links:
    - from: "app/(dashboard)/inventory/page.tsx"
      to: "/inventory/new"
      via: "Link in page header"
      pattern: "Link.*href.*inventory/new"
    - from: "components/requests/request-table.tsx"
      to: "components/data-table/data-table.tsx"
      via: "No exportUrl prop"
      pattern: "exportUrl.*removed"
---

<objective>
Move all CTA buttons and export buttons from DataTable toolbars to page headers (or tab content area headers for admin settings). This ensures consistent button positioning across all list pages.

Purpose: UAT gap closure — user reported that CTA buttons should be in page headers on ALL pages, not just requests/jobs. Export button should also be beside the page title.
Output: All list pages have CTA + export buttons in their page headers. DataTable toolbar no longer renders createButton or exportUrl.
</objective>

<execution_context>
@/Users/melfice/.claude/get-shit-done/workflows/execute-plan.md
@/Users/melfice/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09.1-ui-improvements/09.1-UAT.md
@.planning/phases/09.1-ui-improvements/09.1-01-SUMMARY.md
@components/data-table/data-table-toolbar.tsx
@components/data-table/data-table.tsx
@app/(dashboard)/inventory/page.tsx
@app/(dashboard)/maintenance/templates/page.tsx
@app/(dashboard)/maintenance/page.tsx
@app/(dashboard)/requests/page.tsx
@app/(dashboard)/jobs/page.tsx
@components/assets/asset-table.tsx
@components/maintenance/template-list.tsx
@components/maintenance/schedule-list.tsx
@components/admin/companies/company-table.tsx
@components/admin/divisions/division-table.tsx
@components/admin/locations/location-table.tsx
@components/admin/categories/category-table.tsx
@components/admin/users/user-table.tsx
@components/requests/request-table.tsx
@app/(dashboard)/admin/settings/settings-content.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move CTA buttons to page headers for Inventory, Templates, Schedules and move export to request/job page headers</name>
  <files>
    app/(dashboard)/inventory/page.tsx
    app/(dashboard)/maintenance/templates/page.tsx
    app/(dashboard)/maintenance/page.tsx
    app/(dashboard)/requests/page.tsx
    app/(dashboard)/jobs/page.tsx
    components/assets/asset-table.tsx
    components/maintenance/template-list.tsx
    components/maintenance/schedule-list.tsx
    components/requests/request-table.tsx
    components/data-table/data-table-toolbar.tsx
    components/data-table/data-table.tsx
  </files>
  <action>
    **Inventory page** (`app/(dashboard)/inventory/page.tsx`):
    - Change the existing `<div>` wrapper around h1/p to use `<div className="flex items-center justify-between">` (matching requests/jobs page pattern).
    - Add a `New Asset` button right-aligned beside the title. Use `Button asChild size="sm"` wrapping `Link href="/inventory/new"` with `Plus` icon. Only show for roles `ga_staff`, `ga_lead`, `admin` (the `profile.role` is already available).
    - Import `Link` from `next/link`, `Plus` from `lucide-react`, `Button` from `@/components/ui/button`.

    **Templates page** (`app/(dashboard)/maintenance/templates/page.tsx`):
    - Same pattern: wrap h1/p in `flex items-center justify-between`.
    - Add `New Template` button. Show for `ga_lead` and `admin` roles only.
    - Import `Link`, `Plus`, `Button`.

    **Schedules page** (`app/(dashboard)/maintenance/page.tsx`):
    - Same pattern: wrap h1/p in `flex items-center justify-between`.
    - Add `New Schedule` button. Show for `ga_lead` and `admin` roles only.
    - Import `Link`, `Plus`, `Button`.

    **Remove createButton from asset-table, template-list, schedule-list:**
    - In `components/assets/asset-table.tsx`: Remove the `createButton` prop from the `DataTable` call. Remove `Plus`, `Link`, `Button` imports if no longer used.
    - In `components/maintenance/template-list.tsx`: Remove the `createButton` prop from the `DataTable` call. Remove `Plus`, `Link`, `Button` imports if no longer used.
    - In `components/maintenance/schedule-list.tsx`: Remove the `createButton` prop from the `DataTable` call. Remove `Plus`, `Link`, `Button` imports if no longer used.

    **Move export button to page headers:**
    - In `app/(dashboard)/requests/page.tsx`: Add an export button to the page header, to the LEFT of the existing "New Request" button. Use a client component approach: create a small inline `ExportButton` or use a simple `<a>` or a client island. SIMPLER APPROACH: Since the page is a server component, add the export button as a plain link-style button. Actually, the export needs fetch+blob (not a simple link). Instead, extract the export logic into a tiny client component:
      - Create the export button inline as a `"use client"` component (or better: add it as a prop/slot from the client request-table). SIMPLEST approach: Add the export button directly in the page header using a small client component. Create a reusable `ExportButton` component at `components/export-button.tsx` that accepts `exportUrl: string` and renders the download button with fetch+blob logic. This replaces the export logic in `data-table-toolbar.tsx`.
      - In the requests page header, import and render `<ExportButton exportUrl="/api/exports/requests" />` to the left of the New Request button. Only show for `ga_lead`, `admin`, `finance_approver` roles.
    - In `app/(dashboard)/jobs/page.tsx`: Add `<ExportButton exportUrl="/api/exports/jobs" />` to the left of the New Job button. Only show for `ga_lead`, `admin`, `finance_approver` roles.
    - In `app/(dashboard)/inventory/page.tsx`: Add `<ExportButton exportUrl="/api/exports/inventory" />` to the left of the New Asset button. Only show for `ga_staff`, `ga_lead`, `admin` roles.
    - In `app/(dashboard)/maintenance/page.tsx`: Add `<ExportButton exportUrl="/api/exports/maintenance" />` to the left of the New Schedule button. Only show for `ga_lead`, `admin` roles.
    - Templates page does NOT get an export button (no export route exists for templates).

    **Create ExportButton component** (`components/export-button.tsx`):
    - `"use client"` component.
    - Props: `exportUrl: string`.
    - Renders a `Button variant="outline" size="sm"` with `Download` icon and "Export" text.
    - On click: `fetch(exportUrl)` -> `response.blob()` -> `URL.createObjectURL` -> trigger download via anchor element (same pattern as current data-table-toolbar.tsx handleExport). Include loading state (isExporting) with spinner.

    **Remove exportUrl from request-table.tsx:**
    - In `components/requests/request-table.tsx`: Remove the `exportUrl` prop from the `DataTable` call.

    **Clean up DataTable toolbar:**
    - In `components/data-table/data-table-toolbar.tsx`: Remove `exportUrl` from the props interface. Remove the `handleExport` function. Remove the `isExporting` state. Remove the `exportUrl` rendering block (lines 224-246). Remove the `Download` import from lucide-react if no longer used. Keep `createButton` prop for now since admin settings tabs still use it (will be addressed in Task 2).
    - In `components/data-table/data-table.tsx`: Remove `exportUrl` from the props interface. Remove it from the `DataTableToolbar` call.
  </action>
  <verify>
    <automated>cd /Users/melfice/code/ga && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Visit /requests, /jobs, /inventory, /maintenance — each should have CTA + export buttons in page header (beside title), not in the table toolbar area.</manual>
  </verify>
  <done>All list pages (requests, jobs, inventory, schedules) have CTA buttons in page headers. Export buttons appear beside CTA in page headers for pages with export routes. DataTable toolbar no longer renders exportUrl or its export button.</done>
</task>

<task type="auto">
  <name>Task 2: Move admin settings tab CTA buttons from DataTable toolbar to tab content area headers</name>
  <files>
    components/admin/companies/company-table.tsx
    components/admin/divisions/division-table.tsx
    components/admin/locations/location-table.tsx
    components/admin/categories/category-table.tsx
    components/admin/users/user-table.tsx
    components/data-table/data-table-toolbar.tsx
    components/data-table/data-table.tsx
  </files>
  <action>
    For each of the 5 admin settings tab table components, move the CTA button from the `createButton` prop on `DataTable` to a header area ABOVE the DataTable within the same component.

    **Pattern for each table component:**
    - Remove the `createButton` prop from the `DataTable` call.
    - Add a header row above the `DataTable` (but below the InlineFeedback if present):
      ```tsx
      <div className="flex items-center justify-between">
        <h2 className="text-lg font-semibold">{EntityName}</h2>
        <Button onClick={() => setCreateDialogOpen(true)} size="sm">
          <Plus className="mr-2 h-4 w-4" />
          Create {Entity}
        </Button>
      </div>
      ```
    - The h2 text and button text for each:
      - `company-table.tsx`: h2 "Companies", button "Create Company"
      - `division-table.tsx`: h2 "Divisions", button "Create Division"
      - `location-table.tsx`: h2 "Locations", button "Create Location"
      - `category-table.tsx`: h2 "Categories", button "Create Category"
      - `user-table.tsx`: h2 "Users", button "Create User"
    - Keep all existing imports (Button, Plus are already imported in each).

    **Clean up DataTable toolbar:**
    After all 5 tables are updated, remove the `createButton` prop entirely from `DataTableToolbar` and `DataTable`:
    - In `components/data-table/data-table-toolbar.tsx`: Remove `createButton` from `DataTableToolbarProps` interface and the function params. Remove `{createButton}` from the JSX render (line 247).
    - In `components/data-table/data-table.tsx`: Remove `createButton` from the DataTable props interface and from the `DataTableToolbar` call.

    NOTE: Verify that no other component still passes `createButton` to DataTable. The asset-table, template-list, and schedule-list had theirs removed in Task 1. The request-table and job-table already had theirs removed in 09.1-01. If any remain, remove them.
  </action>
  <verify>
    <automated>cd /Users/melfice/code/ga && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Visit /admin/settings and switch between tabs — each tab should have its Create button above the data table, not inside the toolbar row.</manual>
  </verify>
  <done>All 5 admin settings tab tables have their CTA buttons in a header row above the DataTable. The createButton prop is removed from DataTable and DataTableToolbar entirely. No component passes createButton to DataTable anymore.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. Every list page (requests, jobs, inventory, templates, schedules) has its CTA button in the page header
3. Export buttons appear in page headers for requests, jobs, inventory, and schedules pages
4. Admin settings tabs show Create buttons above DataTable, not in toolbar
5. DataTable and DataTableToolbar no longer accept createButton or exportUrl props
6. No regressions — existing functionality (bulk delete, bulk export, search, filters) unchanged
</verification>

<success_criteria>
- All CTA buttons consistently positioned in page headers across the entire application
- Export buttons in page headers on pages with export routes
- DataTable toolbar is clean — only search, filters, deactivated toggle, and bulk actions remain
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-ui-improvements/09.1-04-SUMMARY.md`
</output>
