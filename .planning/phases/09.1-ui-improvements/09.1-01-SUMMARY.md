---
phase: 09.1-ui-improvements
plan: 01
subsystem: ui
tags: [react, tanstack-table, tailwind, lucide-icons]

# Dependency graph
requires:
  - phase: 04-requests
    provides: "Request list page, request-columns.tsx, request-table.tsx"
  - phase: 05-jobs-approvals
    provides: "Job list page, job-columns.tsx, job-table.tsx"
provides:
  - "Compact request columns with status beside ID, ghost action buttons, no category column"
  - "Compact job columns with status beside ID, ghost action buttons, vertically stacked linked requests"
  - "CTA buttons in page headers for requests and jobs"
affects: [09.1-ui-improvements]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Ghost icon buttons for row actions instead of dropdown menus"
    - "CTA button in page header (beside h1) instead of DataTable toolbar"
    - "Status badge merged into ID column cell for compact table layout"

key-files:
  created: []
  modified:
    - "components/requests/request-columns.tsx"
    - "components/jobs/job-columns.tsx"
    - "components/requests/request-table.tsx"
    - "components/jobs/job-table.tsx"
    - "app/(dashboard)/requests/page.tsx"
    - "app/(dashboard)/jobs/page.tsx"

key-decisions:
  - "Ghost icon buttons with title attributes for accessibility instead of Tooltip components -- simpler, lower overhead"
  - "CTA buttons rendered in server component page headers rather than client component DataTable toolbar -- cleaner separation"
  - "Job edit button navigates to detail page (inline editing) not separate edit route -- consistent with CLAUDE.md convention"

patterns-established:
  - "Row actions pattern: visible ghost icon buttons (h-7 w-7, icon h-3.5 w-3.5) with stopPropagation and title attribute"
  - "Page header pattern: flex items-center justify-between with h1 left, CTA button right"

requirements-completed: []

# Metrics
duration: 3min
completed: 2026-02-27
---

# Phase 09.1 Plan 01: List Page Column Overhaul Summary

**Compact request/job list pages with CTA in page headers, status beside ID, ghost action buttons, and vertically stacked linked requests**

## Performance

- **Duration:** 3 min
- **Started:** 2026-02-27T06:41:51Z
- **Completed:** 2026-02-27T06:45:39Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments
- Moved New Request / New Job CTA buttons from DataTable toolbar to page headers (right-aligned beside title)
- Removed Category column from request list for compactness
- Merged status badge into ID column cell on both request and job list tables
- Replaced three-dot dropdown menus with visible ghost icon buttons on both tables
- Stacked linked request chips vertically in job list instead of truncating with +N
- Compacted column sizes across both tables

## Task Commits

Each task was committed atomically:

1. **Task 1: Move CTA buttons to page headers and overhaul request columns** - `b091fc4` (feat)
2. **Task 2: Overhaul job columns -- status beside ID, ghost buttons, vertical linked requests** - `b451dd4` (feat)

## Files Created/Modified
- `components/requests/request-columns.tsx` - Removed category column, merged status into ID, replaced dropdown with ghost buttons, compacted sizes
- `components/jobs/job-columns.tsx` - Merged status into ID, replaced dropdown with ghost buttons, vertical linked requests, compacted sizes
- `components/requests/request-table.tsx` - Removed createButton prop from DataTable
- `components/jobs/job-table.tsx` - Removed createButton prop and isGaLeadOrAdmin variable, fixed edit navigation to detail page
- `app/(dashboard)/requests/page.tsx` - Added New Request button in page header
- `app/(dashboard)/jobs/page.tsx` - Added New Job button in page header (GA Lead/Admin only)

## Decisions Made
- Used ghost icon buttons with `title` attributes for accessibility rather than shadcn Tooltip components -- simpler approach with minimal overhead
- Rendered CTA buttons in server component page headers rather than client component DataTable toolbar -- cleaner separation of concerns
- Fixed job "Edit" navigation to go to detail page (`/jobs/${id}`) instead of non-existent `/jobs/${id}/edit` route -- consistent with CLAUDE.md "detail pages ARE edit pages" convention

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed job edit navigation to detail page**
- **Found during:** Task 1 (job-table.tsx cleanup)
- **Issue:** `handleEdit` navigated to `/jobs/${job.id}/edit` which doesn't exist -- detail page handles inline editing
- **Fix:** Changed navigation target to `/jobs/${job.id}`
- **Files modified:** components/jobs/job-table.tsx
- **Verification:** No separate edit route exists; detail page handles editing inline per CLAUDE.md convention
- **Committed in:** b091fc4 (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 bug fix)
**Impact on plan:** Auto-fix necessary for correctness. No scope creep.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- List page column overhaul complete for requests and jobs
- Ready for plan 02 (additional UI improvements)

---
*Phase: 09.1-ui-improvements*
*Completed: 2026-02-27*
