---
phase: 08-media-notifications-dashboards
plan: 06
type: execute
wave: 2
depends_on: ["08-05"]
files_modified:
  - components/dashboard/status-bar-chart.tsx
  - components/dashboard/staff-workload-table.tsx
  - components/dashboard/request-aging-table.tsx
  - components/dashboard/maintenance-summary.tsx
  - components/dashboard/inventory-summary.tsx
  - lib/dashboard/queries.ts
  - app/(dashboard)/page.tsx
autonomous: true
requirements:
  - REQ-DASH-002
  - REQ-DASH-003
  - REQ-DASH-004
  - REQ-DASH-005
  - REQ-DASH-006
  - REQ-DASH-007

must_haves:
  truths:
    - "Request status distribution is shown as a horizontal bar chart"
    - "Job status distribution is shown as a horizontal bar chart"
    - "Staff workload table shows active jobs, completed, and overdue per GA Staff member"
    - "Request aging table shows counts per bucket (0-3, 4-7, 8-14, 15+ days)"
    - "Maintenance due/overdue summary lists items grouped by urgency"
    - "Inventory counts shown by status and category"
    - "Clicking chart segments navigates to filtered list pages"
  artifacts:
    - path: "components/dashboard/status-bar-chart.tsx"
      provides: "Recharts horizontal bar chart for status distribution"
      contains: "StatusBarChart"
    - path: "components/dashboard/staff-workload-table.tsx"
      provides: "Sortable staff workload table"
      contains: "StaffWorkloadTable"
    - path: "components/dashboard/request-aging-table.tsx"
      provides: "Request aging buckets table"
      contains: "RequestAgingTable"
    - path: "components/dashboard/maintenance-summary.tsx"
      provides: "Maintenance due/overdue grouped list"
      contains: "MaintenanceSummary"
    - path: "components/dashboard/inventory-summary.tsx"
      provides: "Inventory counts by status/category"
      contains: "InventorySummary"
  key_links:
    - from: "components/dashboard/status-bar-chart.tsx"
      to: "recharts"
      via: "BarChart with layout='vertical'"
      pattern: "BarChart.*layout.*vertical"
    - from: "app/(dashboard)/page.tsx"
      to: "lib/dashboard/queries.ts"
      via: "server-side data fetching for charts"
      pattern: "getStatusDistribution|getStaffWorkload"
---

<objective>
Build all dashboard visualization components: status distribution charts, staff workload table, request aging table, maintenance summary, and inventory counts. Wire them into the dashboard page.

Purpose: Beyond KPI cards, the dashboard needs detailed visualizations for operational decision-making — where work is stuck, who is overloaded, what maintenance is overdue.
Output: 5 dashboard visualization components, expanded query functions, updated dashboard page
</objective>

<execution_context>
@/Users/melfice/.claude/get-shit-done/workflows/execute-plan.md
@/Users/melfice/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-media-notifications-dashboards/08-CONTEXT.md
@.planning/phases/08-media-notifications-dashboards/08-RESEARCH.md
@.planning/phases/08-media-notifications-dashboards/08-05-SUMMARY.md

@lib/dashboard/queries.ts (existing query functions from plan 08-05)
@app/(dashboard)/page.tsx (dashboard page to expand)
@lib/constants/request-status.ts (status labels and colors)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create status bar charts and staff workload table</name>
  <files>
    components/dashboard/status-bar-chart.tsx
    components/dashboard/staff-workload-table.tsx
    lib/dashboard/queries.ts
  </files>
  <action>
1. Create `components/dashboard/status-bar-chart.tsx`:
   - `'use client'` component (recharts requires client-side rendering)
   - Props: `data: { status: string, label: string, count: number, color: string }[]`, `entityPath: string` (e.g., 'requests' or 'jobs'), `title: string`
   - Use recharts `BarChart` with `layout="vertical"` for horizontal bars (per user decision — horizontal bars, NOT donut/pie)
   - Wrap in `ResponsiveContainer` inside a div with explicit height (`h-[250px]`)
   - `XAxis` type="number", `YAxis` type="category" dataKey="label" width={100}
   - `Bar` dataKey="count" with `Cell` components for per-bar colors (use STATUS_COLORS mapped to hex)
   - Click handler on bars: `router.push(/${entityPath}?status=${entry.status})` — per user decision, clicking a segment navigates to filtered list
   - Tooltip showing count
   - Title rendered above the chart as `<h3>` with card wrapper

2. Create `components/dashboard/staff-workload-table.tsx`:
   - `'use client'` component
   - Props: `data: { staffName: string, staffId: string, activeJobs: number, completedThisMonth: number, overdue: number }[]`
   - Per user decision: table with columns — Staff name, Active jobs, Completed (this month), Overdue. Sortable.
   - Use a simple HTML table with shadcn Table components (Table, TableHeader, TableRow, TableCell)
   - Sortable: useState for sort column and direction, sort the data array before rendering
   - Column headers clickable with sort indicator arrows
   - Overdue column: red text if > 0
   - Card wrapper with title "Staff Workload"

3. Add query functions to `lib/dashboard/queries.ts`:
   - `getRequestStatusDistribution(supabase, dateRange)`:
     - For each status in REQUEST_STATUSES, count requests with that status within dateRange (based on created_at)
     - Return array of `{ status, label, count, color }` using STATUS_LABELS and STATUS_COLORS
   - `getJobStatusDistribution(supabase, dateRange)`:
     - Same pattern for job statuses: 'created', 'assigned', 'in_progress', 'completed'
     - Use appropriate labels and colors
   - `getStaffWorkload(supabase)`:
     - Query user_profiles where role IN ('ga_staff', 'ga_lead') and deleted_at IS NULL
     - For each staff member, count: active jobs (status IN created/assigned/in_progress), completed this month, overdue (in_progress and older than 7 days)
     - Consider using a single query with GROUP BY assigned_to for efficiency:
       ```sql
       select assigned_to, status, count from jobs where deleted_at IS NULL group by assigned_to, status
       ```
     - Then aggregate in JS
     - Return array of staff workload objects
  </action>
  <verify>
    <automated>cd /Users/melfice/code/ga && test -f components/dashboard/status-bar-chart.tsx && test -f components/dashboard/staff-workload-table.tsx && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>StatusBarChart renders horizontal bars per status with click navigation. StaffWorkloadTable is sortable with correct columns.</manual>
  </verify>
  <done>Horizontal bar charts for request and job status distribution with clickable segments. Staff workload table with sortable columns (Staff name, Active jobs, Completed, Overdue). Query functions fetch distribution and workload data.</done>
</task>

<task type="auto">
  <name>Task 2: Create aging, maintenance, inventory components and wire into dashboard</name>
  <files>
    components/dashboard/request-aging-table.tsx
    components/dashboard/maintenance-summary.tsx
    components/dashboard/inventory-summary.tsx
    lib/dashboard/queries.ts
    app/(dashboard)/page.tsx
  </files>
  <action>
1. Create `components/dashboard/request-aging-table.tsx`:
   - Server component (no interaction needed, just display)
   - Props: `data: { bucket: string, count: number }[]`
   - Per user decision: buckets table — 0-3 days, 4-7 days, 8-14 days, 15+ days (count of open requests per bucket)
   - Simple table with 4 columns, one per bucket
   - 15+ days bucket: red/orange text if count > 0 to highlight aging
   - Card wrapper with title "Request Aging"

2. Create `components/dashboard/maintenance-summary.tsx`:
   - Props: `data: { id: string, assetName: string, templateName: string, dueDate: string, urgency: 'overdue' | 'due_this_week' | 'due_this_month' }[]`
   - Per user decision: list grouped by urgency — overdue at top (red bg), due this week (yellow bg), due this month (normal bg)
   - Each item shows: asset name + template name + due date (dd-MM-yyyy format)
   - If no items: "No maintenance due" message
   - Card wrapper with title "Maintenance Summary"

3. Create `components/dashboard/inventory-summary.tsx`:
   - Props: `data: { status: string, count: number }[]` and `byCategory: { category: string, count: number }[]`
   - Per user decision: inventory counts by status AND by category
   - Two small tables side by side (grid-cols-2 max-md:grid-cols-1)
   - Status table: Active, Under Repair, Broken, Sold/Disposed with counts
   - Category table: each asset category with count
   - Card wrapper with title "Inventory Overview"

4. Add query functions to `lib/dashboard/queries.ts`:
   - `getRequestAging(supabase)`:
     - Fetch all open requests (status IN submitted/triaged/in_progress)
     - Calculate age from created_at to now in JS using date-fns differenceInDays
     - Bucket into 0-3, 4-7, 8-14, 15+ day ranges
     - Return `{ bucket, count }[]`
   - `getMaintenanceSummary(supabase)`:
     - Query maintenance_schedules with is_active = true, join to assets and templates
     - Compare next_due_at to now: overdue if past, due_this_week if within 7 days, due_this_month if within 30 days
     - Return sorted by urgency (overdue first)
   - `getInventoryCounts(supabase)`:
     - Count inventory_items grouped by status (GROUP BY)
     - Count inventory_items grouped by category (join to categories for name)
     - Return both arrays

5. Update `app/(dashboard)/page.tsx`:
   - After KPI cards, add a two-column grid for charts and tables:
     ```
     grid grid-cols-2 max-lg:grid-cols-1 gap-6
     ```
   - Left column: Request Status Distribution chart, Job Status Distribution chart (stacked vertically)
   - Right column: Staff Workload table, Request Aging table (stacked vertically)
   - Below the two-column grid: full-width row with Maintenance Summary and Inventory Summary side by side
   - Fetch all data in parallel in the server component using Promise.all
   - Pass data as props to each visualization component
   - Only show for operational roles (ga_lead, admin, finance_approver)
  </action>
  <verify>
    <automated>cd /Users/melfice/code/ga && test -f components/dashboard/request-aging-table.tsx && test -f components/dashboard/maintenance-summary.tsx && test -f components/dashboard/inventory-summary.tsx && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Dashboard shows all visualizations: charts, workload, aging, maintenance, inventory. Desktop-first grid layout.</manual>
  </verify>
  <done>Dashboard page shows complete operational view: status distribution charts (horizontal bars), staff workload (sortable table), request aging (bucket counts), maintenance summary (urgency-grouped list), inventory counts (by status and category). All data fetched in parallel server-side.</done>
</task>

</tasks>

<verification>
1. Request and job status distribution shown as horizontal bar charts
2. Clicking a chart bar navigates to filtered list page
3. Staff workload table with sortable columns
4. Request aging table with 4 buckets (0-3, 4-7, 8-14, 15+ days)
5. Maintenance summary grouped by urgency (overdue red, due this week yellow)
6. Inventory counts by status and by category
7. Dashboard layout: desktop-first grid, responsive down to mobile
8. All data fetched in parallel on server side
9. TypeScript compiles without errors
</verification>

<success_criteria>
- All 6 dashboard requirements addressed (DASH-002 through DASH-007)
- Horizontal bar charts with clickable segments for request and job status
- Sortable staff workload table
- Aging buckets table with visual emphasis on old items
- Maintenance summary with color-coded urgency
- Inventory counts by status and category
- Desktop-first responsive layout
</success_criteria>

<output>
After completion, create `.planning/phases/08-media-notifications-dashboards/08-06-SUMMARY.md`
</output>
