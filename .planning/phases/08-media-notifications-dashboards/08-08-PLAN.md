---
phase: 08-media-notifications-dashboards
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - app/actions/job-actions.ts
  - app/actions/approval-actions.ts
autonomous: true
gap_closure: true
requirements:
  - REQ-NOTIF-006
  - REQ-NOTIF-007
  - REQ-DASH-001
  - REQ-DASH-002
  - REQ-DASH-003
  - REQ-DASH-004
  - REQ-DASH-005
  - REQ-DASH-006
  - REQ-DASH-007
  - REQ-DATA-002
  - REQ-MEDIA-001
  - REQ-MEDIA-002
  - REQ-MEDIA-003
  - REQ-MEDIA-004
  - REQ-MEDIA-005
  - REQ-MEDIA-006
  - REQ-NOTIF-001
  - REQ-NOTIF-002
  - REQ-NOTIF-003
  - REQ-NOTIF-004
  - REQ-NOTIF-005
  - REQ-NOTIF-007

must_haves:
  truths:
    - "Job assignment triggers a notification to the assigned GA Staff member"
    - "Job completion triggers a notification to linked request requesters with auto-accept warning"
    - "Submitting a job for approval triggers a notification to Finance Approver users"
    - "Approving a job triggers a notification to the job creator and assigned PIC"
    - "Rejecting an approval triggers a notification to the job creator and assigned PIC with reason"
    - "The actor is never included in notification recipients"
  artifacts:
    - path: "app/actions/job-actions.ts"
      provides: "Notification triggers for job assignment, completion, and cancellation"
      contains: "createNotifications"
    - path: "app/actions/approval-actions.ts"
      provides: "Notification triggers for submitForApproval, approveJob, rejectJob"
      contains: "createNotifications"
  key_links:
    - from: "app/actions/job-actions.ts"
      to: "lib/notifications/helpers.ts"
      via: "import createNotifications"
      pattern: "import.*createNotifications.*from.*notifications/helpers"
    - from: "app/actions/approval-actions.ts"
      to: "lib/notifications/helpers.ts"
      via: "import createNotifications"
      pattern: "import.*createNotifications.*from.*notifications/helpers"
---

<objective>
Wire notification triggers into job-actions.ts and approval-actions.ts to close the partial coverage gap identified in 08-VERIFICATION.md.

Purpose: REQ-NOTIF-006 requires notifications for status changes, assignments, approvals, completions, and auto-accept warnings. Currently only request-actions.ts fires notifications (triage/cancel/reject). Job and approval actions have no notification triggers at all.

Output: Both job-actions.ts and approval-actions.ts call createNotifications after relevant mutations, completing notification event coverage.
</objective>

<execution_context>
@/Users/melfice/.claude/get-shit-done/workflows/execute-plan.md
@/Users/melfice/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-media-notifications-dashboards/08-VERIFICATION.md
@.planning/phases/08-media-notifications-dashboards/08-03-SUMMARY.md
@.planning/phases/08-media-notifications-dashboards/08-04-SUMMARY.md
@lib/notifications/helpers.ts
@app/actions/request-actions.ts (reference pattern for how createNotifications is called)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire notification triggers into job-actions.ts</name>
  <files>app/actions/job-actions.ts</files>
  <action>
Add `import { createNotifications } from '@/lib/notifications/helpers';` at the top of the file.

Wire createNotifications into these actions, following the exact fire-and-forget pattern from request-actions.ts (non-blocking, no await, .catch(() => {}) is NOT needed since createNotifications already has internal try/catch):

1. **assignJob** — After successful assignment update, fetch the job's display_id and notify the assigned user:
   - Fetch the job with `display_id` in the initial select (change `.select('id, status')` to `.select('id, status, display_id')`).
   - After the successful update, call:
     ```
     createNotifications({
       companyId: profile.company_id,
       recipientIds: [parsedInput.assigned_to],
       actorId: profile.id,
       title: `Job ${job.display_id} assigned to you`,
       body: 'You have been assigned as PIC for this job',
       type: 'assignment',
       entityType: 'job',
       entityId: parsedInput.id,
     });
     ```

2. **updateJobStatus** (when status = 'completed') — After moving linked requests to pending_acceptance, notify each requester with an auto-accept warning:
   - After the linked request update block (line ~371), fetch the requester IDs for those linked requests:
     ```
     const { data: linkedRequests } = await supabase
       .from('requests')
       .select('requester_id, display_id')
       .in('id', requestIds)
       .is('deleted_at', null);
     ```
   - For each linked request, notify the requester:
     ```
     if (linkedRequests && linkedRequests.length > 0) {
       const requesterIds = [...new Set(linkedRequests.map(r => r.requester_id))];
       createNotifications({
         companyId: job.company_id,
         recipientIds: requesterIds,
         actorId: profile.id,
         title: `Job completed — please review`,
         body: 'Your request work is done. Accept or reject within 7 days or it will be auto-accepted.',
         type: 'auto_accept_warning',
         entityType: 'job',
         entityId: parsedInput.id,
       });
     }
     ```
   - Also notify the job creator (if different from PIC) about completion:
     - Fetch the job's `created_by` and `display_id` in the initial select (change `.select('id, status, assigned_to, company_id')` to `.select('id, status, assigned_to, company_id, created_by, display_id')`).
     - After the job update succeeds, add:
       ```
       const completionRecipients = [job.created_by, job.assigned_to].filter(Boolean) as string[];
       createNotifications({
         companyId: job.company_id,
         recipientIds: completionRecipients,
         actorId: profile.id,
         title: `Job ${job.display_id} completed`,
         type: 'completion',
         entityType: 'job',
         entityId: parsedInput.id,
       });
       ```

3. **cancelJob** — After successful cancellation, notify the assigned PIC (if any):
   - Fetch assigned_to and display_id in the initial select (change `.select('id, status')` to `.select('id, status, assigned_to, display_id, company_id')`). Note: company_id is already available via the eq filter, but we need it on the object for the notification call.
   - After the cancellation update, if job.assigned_to exists:
     ```
     if (job.assigned_to) {
       createNotifications({
         companyId: profile.company_id,
         recipientIds: [job.assigned_to],
         actorId: profile.id,
         title: `Job ${job.display_id} cancelled`,
         type: 'status_change',
         entityType: 'job',
         entityId: parsedInput.id,
       });
     }
     ```

Do NOT add notifications to createJob or updateJob — these are configuration/edit actions, not workflow transitions. Also do NOT add notifications to addJobComment (comment notifications are a v2 feature per REQ-NOTIF-010).
  </action>
  <verify>
    <automated>cd /Users/melfice/code/ga && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Verify createNotifications import exists and is called in assignJob, updateJobStatus (completed branch), and cancelJob</manual>
  </verify>
  <done>job-actions.ts imports createNotifications and calls it in assignJob (type=assignment), updateJobStatus completion branch (type=completion + type=auto_accept_warning for requesters), and cancelJob (type=status_change). Actor is always excluded via actorId param.</done>
</task>

<task type="auto">
  <name>Task 2: Wire notification triggers into approval-actions.ts</name>
  <files>app/actions/approval-actions.ts</files>
  <action>
Add `import { createNotifications } from '@/lib/notifications/helpers';` at the top of the file.

Wire createNotifications into all three actions:

1. **submitForApproval** — After successful status update to pending_approval, notify Finance Approver users:
   - After `revalidatePath('/approvals')`, fetch finance approvers for this company:
     ```
     const { data: financeApprovers } = await supabase
       .from('user_profiles')
       .select('id')
       .eq('company_id', job.company_id)
       .in('role', ['finance_approver', 'admin'])
       .is('deleted_at', null);
     ```
   - Fetch the job's display_id in the initial select (change `.select('id, status, estimated_cost, company_id')` to `.select('id, status, estimated_cost, company_id, display_id')`).
   - Call createNotifications:
     ```
     if (financeApprovers && financeApprovers.length > 0) {
       createNotifications({
         companyId: job.company_id,
         recipientIds: financeApprovers.map(u => u.id),
         actorId: profile.id,
         title: `Job ${job.display_id} requires approval`,
         body: `Estimated cost: Rp ${(estimatedCost).toLocaleString('id-ID')}`,
         type: 'approval',
         entityType: 'job',
         entityId: parsedInput.job_id,
       });
     }
     ```
   - Place this BEFORE the `return { success: true }` but AFTER the revalidatePath calls.

2. **approveJob** — After successful approval, notify the job creator and assigned PIC:
   - Expand the initial select to include creator and PIC: change `.select('id, status, company_id')` to `.select('id, status, company_id, display_id, created_by, assigned_to')`.
   - After the status update succeeds, add:
     ```
     const approvalRecipients = [job.created_by, job.assigned_to].filter(Boolean) as string[];
     createNotifications({
       companyId: job.company_id,
       recipientIds: approvalRecipients,
       actorId: profile.id,
       title: `Job ${job.display_id} approved`,
       body: 'Budget approval granted — work can proceed',
       type: 'approval',
       entityType: 'job',
       entityId: parsedInput.job_id,
     });
     ```
   - Place this BEFORE the `return { success: true }`.

3. **rejectJob** — After successful rejection, notify the job creator and assigned PIC with reason:
   - Expand the initial select: change `.select('id, status, company_id')` to `.select('id, status, company_id, display_id, created_by, assigned_to')`.
   - After the status update succeeds, add:
     ```
     const rejectionRecipients = [job.created_by, job.assigned_to].filter(Boolean) as string[];
     const truncatedReason = parsedInput.reason.substring(0, 100);
     createNotifications({
       companyId: job.company_id,
       recipientIds: rejectionRecipients,
       actorId: profile.id,
       title: `Job ${job.display_id} approval rejected`,
       body: truncatedReason,
       type: 'approval',
       entityType: 'job',
       entityId: parsedInput.job_id,
     });
     ```
   - Place this BEFORE the `return { success: true }`.

Note: All calls are non-blocking. createNotifications has internal try/catch so failures never break the triggering action. The actorId parameter ensures the actor is never notified about their own action (REQ-NOTIF-007).
  </action>
  <verify>
    <automated>cd /Users/melfice/code/ga && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Verify createNotifications import exists and is called in submitForApproval, approveJob, and rejectJob</manual>
  </verify>
  <done>approval-actions.ts imports createNotifications and calls it in submitForApproval (type=approval, notifies finance_approver+admin), approveJob (type=approval, notifies creator+PIC), and rejectJob (type=approval, notifies creator+PIC with reason). Actor is always excluded via actorId param.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors: `npx tsc --noEmit`
2. `grep -c 'createNotifications' app/actions/job-actions.ts` returns >= 3 (one import + at least 3 calls)
3. `grep -c 'createNotifications' app/actions/approval-actions.ts` returns >= 3 (one import + 3 calls)
4. All five notification types used: status_change, assignment, approval, completion, auto_accept_warning
5. Every createNotifications call includes actorId: profile.id
</verification>

<success_criteria>
- job-actions.ts fires notifications for: assignment (assignJob), completion (updateJobStatus), cancellation (cancelJob), auto-accept warning (to requesters when job completes)
- approval-actions.ts fires notifications for: submitForApproval, approveJob, rejectJob
- All five notification types from the helpers.ts type union are used across the codebase
- Actor is never notified about their own action (actorId always set)
- TypeScript compiles cleanly
- REQ-NOTIF-006 fully satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/08-media-notifications-dashboards/08-08-SUMMARY.md`
</output>
