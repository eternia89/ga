---
phase: 08-media-notifications-dashboards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/media/compression.ts
  - components/media/photo-upload.tsx
  - components/media/photo-annotation.tsx
  - app/api/uploads/entity-photos/route.ts
  - package.json
  - package-lock.json
autonomous: true
requirements:
  - REQ-MEDIA-001
  - REQ-MEDIA-002
  - REQ-MEDIA-003
  - REQ-MEDIA-005

must_haves:
  truths:
    - "Images are compressed to WebP format (max 800KB) client-side before upload"
    - "Upload route accepts any entity type (request, job, inventory, job_comment) with company-scoped storage paths"
    - "Users can annotate images with freehand drawing before uploading"
    - "Up to 10 images can be uploaded per entity"
  artifacts:
    - path: "lib/media/compression.ts"
      provides: "Client-side image compression wrapper using browser-image-compression"
      contains: "compressImage"
    - path: "components/media/photo-upload.tsx"
      provides: "Generic photo upload component with compression and annotation support"
      contains: "PhotoUpload"
    - path: "components/media/photo-annotation.tsx"
      provides: "Freehand drawing annotation dialog using react-sketch-canvas"
      contains: "PhotoAnnotation"
    - path: "app/api/uploads/entity-photos/route.ts"
      provides: "Generalized entity photo upload API route"
      exports: ["POST"]
  key_links:
    - from: "components/media/photo-upload.tsx"
      to: "lib/media/compression.ts"
      via: "import compressImage"
      pattern: "compressImage"
    - from: "components/media/photo-upload.tsx"
      to: "components/media/photo-annotation.tsx"
      via: "annotation dialog trigger"
      pattern: "PhotoAnnotation"
    - from: "components/media/photo-upload.tsx"
      to: "app/api/uploads/entity-photos/route.ts"
      via: "FormData POST"
      pattern: "entity-photos"
---

<objective>
Build the core image handling infrastructure: client-side WebP compression, a generalized entity upload route replacing the request-specific one, a freehand annotation dialog, and a reusable photo upload component.

Purpose: All entity types (requests, jobs, inventory, maintenance) need image upload capability. This plan creates the shared media infrastructure that all subsequent photo features build on.
Output: compression utility, annotation dialog, generic upload route, reusable photo upload component
</objective>

<execution_context>
@/Users/melfice/.claude/get-shit-done/workflows/execute-plan.md
@/Users/melfice/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-media-notifications-dashboards/08-CONTEXT.md
@.planning/phases/08-media-notifications-dashboards/08-RESEARCH.md

@app/api/uploads/request-photos/route.ts (existing upload pattern to generalize)
@components/requests/request-photo-upload.tsx (existing photo upload to replace with generic)
@lib/supabase/admin.ts (admin client for storage writes)
@lib/supabase/server.ts (auth client for user validation)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install media libraries and create compression + annotation components</name>
  <files>
    package.json
    package-lock.json
    lib/media/compression.ts
    components/media/photo-annotation.tsx
  </files>
  <action>
1. Install required packages: `npm install browser-image-compression react-sketch-canvas`

2. Create `lib/media/compression.ts`:
   - Export `compressImage(file: File): Promise<File>` function
   - Use `browser-image-compression` with options: `maxSizeMB: 0.8`, `maxWidthOrHeight: 1920`, `useWebWorker: true`, `fileType: 'image/webp'`, `initialQuality: 0.85`, `preserveExif: false`
   - Add a `'use client'` directive at top (this runs in the browser)
   - Return the compressed file

3. Create `components/media/photo-annotation.tsx`:
   - `'use client'` component
   - Import `ReactSketchCanvas` and `ReactSketchCanvasRef` from `react-sketch-canvas`
   - Props: `imageUrl: string`, `onSave: (file: File) => void`, `onCancel: () => void`, `open: boolean`
   - Render inside a shadcn Dialog (full-screen overlay, use `DialogContent` with custom class for large size)
   - Set `backgroundImage={imageUrl}`, `exportWithBackgroundImage={true}`, `strokeWidth={4}`, `strokeColor="red"`
   - Provide toolbar buttons: color picker (preset colors: red, blue, yellow, white, black), stroke width slider (2-8), Undo, Clear, Cancel, Save
   - On Save: call `canvasRef.current.exportImage('png')` to get data URL, convert to File via `fetch(dataUrl).then(r => r.blob())`, call `onSave(file)` — the caller will run it through compression
   - Freehand drawing ONLY per user decision — no text overlay, no shapes, no arrows
  </action>
  <verify>
    <automated>cd /Users/melfice/code/ga && node -e "require('browser-image-compression'); console.log('browser-image-compression OK')" && node -e "require('react-sketch-canvas'); console.log('react-sketch-canvas OK')" && test -f lib/media/compression.ts && test -f components/media/photo-annotation.tsx && echo "All files exist"</automated>
    <manual>Verify compression.ts exports compressImage, annotation component has Dialog + ReactSketchCanvas</manual>
  </verify>
  <done>browser-image-compression and react-sketch-canvas installed. Compression utility exports compressImage with WebP/800KB config. Annotation dialog renders full-screen canvas with freehand drawing and exports merged image.</done>
</task>

<task type="auto">
  <name>Task 2: Create generalized entity upload route and reusable photo upload component</name>
  <files>
    app/api/uploads/entity-photos/route.ts
    components/media/photo-upload.tsx
  </files>
  <action>
1. Create `app/api/uploads/entity-photos/route.ts`:
   - Generalize the existing `app/api/uploads/request-photos/route.ts` pattern
   - Define ENTITY_CONFIGS map:
     ```
     request: { bucket: 'request-photos', maxFiles: 10 }
     job: { bucket: 'job-photos', maxFiles: 10 }
     inventory: { bucket: 'inventory-photos', maxFiles: 10 }
     job_comment: { bucket: 'job-photos', maxFiles: 3 }
     ```
   - Accept FormData with fields: `entity_type`, `entity_id`, `photos` (files)
   - Auth validation: getUser(), fetch profile, check not deactivated
   - Entity ownership validation: For requests — check requester owns it and status is 'submitted'. For other entity types — check company_id matches user's company (since jobs/inventory may have different ownership rules, keep validation generic for now: verify the entity exists in the entity's table and belongs to user's company)
   - Storage path format: `{company_id}/{entity_type}/{entity_id}/{uuid}-{sanitized_filename}`
   - Use `createAdminClient()` for storage writes (service_role bypasses storage RLS)
   - Insert `media_attachments` records with adminSupabase (same pattern as existing route)
   - MAX_SIZE_BYTES = 5MB, ALLOWED_MIME_TYPES = ['image/jpeg', 'image/png', 'image/webp']
   - Check existing attachment count against config maxFiles before uploading

2. Create `components/media/photo-upload.tsx`:
   - `'use client'` component
   - Props: `entityType: string`, `entityId: string`, `onChange: (files: File[]) => void`, `existingPhotos?: ExistingPhoto[]`, `disabled?: boolean`, `maxPhotos?: number` (default 10), `enableAnnotation?: boolean` (default true)
   - Replaces/extends the pattern from `components/requests/request-photo-upload.tsx`
   - Before adding a file to previews, run it through `compressImage()` from `lib/media/compression.ts`
   - When user clicks an "annotate" icon on a preview thumbnail, open `PhotoAnnotation` dialog. On save, replace the preview with the annotated+compressed result.
   - Thumbnail grid: 80x80px thumbnails with remove button (X), plus "Add" button when slots remain
   - Annotation button: small pencil icon overlay on each preview thumbnail
   - Export `PhotoUpload` and `ExistingPhoto` type
   - Note: The existing `RequestPhotoUpload` in `components/requests/request-photo-upload.tsx` should NOT be modified — the new generic component will be used for new features, and existing request code can be migrated later if needed
  </action>
  <verify>
    <automated>cd /Users/melfice/code/ga && test -f app/api/uploads/entity-photos/route.ts && test -f components/media/photo-upload.tsx && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Upload route handles multiple entity types. PhotoUpload component compresses before preview and supports annotation.</manual>
  </verify>
  <done>Entity-agnostic upload route accepts request/job/inventory/job_comment entity types with company-scoped paths. Reusable PhotoUpload component compresses images to WebP (max 800KB) and offers freehand annotation before upload.</done>
</task>

</tasks>

<verification>
1. `browser-image-compression` and `react-sketch-canvas` are in package.json dependencies
2. `lib/media/compression.ts` exports `compressImage` with WebP/800KB config
3. `components/media/photo-annotation.tsx` renders ReactSketchCanvas with freehand-only drawing
4. `app/api/uploads/entity-photos/route.ts` handles multiple entity types with ENTITY_CONFIGS
5. `components/media/photo-upload.tsx` compresses images before preview and triggers annotation dialog
6. TypeScript compiles without errors
</verification>

<success_criteria>
- Images compressed client-side to WebP (max 800KB) via browser-image-compression
- Generalized upload route serves all entity types (request, job, inventory, job_comment)
- Freehand annotation dialog works (draw over photo, export merged image)
- PhotoUpload component integrates compression + annotation into upload flow
- Up to 10 photos per entity (configurable)
</success_criteria>

<output>
After completion, create `.planning/phases/08-media-notifications-dashboards/08-01-SUMMARY.md`
</output>
