---
phase: 03-admin-system-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - components.json
  - components/ui/*.tsx
  - lib/utils.ts
  - app/globals.css
  - components/data-table/data-table.tsx
  - components/data-table/data-table-pagination.tsx
  - components/data-table/data-table-toolbar.tsx
  - components/data-table/data-table-column-header.tsx
  - components/delete-confirm-dialog.tsx
  - components/inline-feedback.tsx
  - components/sidebar.tsx
  - components/user-menu.tsx
  - app/(dashboard)/layout.tsx
  - app/(dashboard)/admin/settings/page.tsx
  - app/(dashboard)/admin/layout.tsx
  - lib/safe-action.ts
autonomous: true

must_haves:
  truths:
    - "UI components render with consistent shadcn/ui design system styling across all admin pages"
    - "A reusable DataTable component renders data with sorting, filtering, pagination, row selection, and bulk action toolbar"
    - "The Settings page at /admin/settings renders with four tabs (Companies, Divisions, Locations, Categories) controlled by URL query parameter"
    - "The sidebar shows Settings and Users as active clickable links (not 'Coming soon') for admin role"
    - "Admin layout at /admin wraps Settings and Users pages with permission gate"
  artifacts:
    - path: "components/data-table/data-table.tsx"
      provides: "Reusable data table with TanStack Table"
      min_lines: 50
    - path: "app/(dashboard)/admin/settings/page.tsx"
      provides: "Settings page with tabbed navigation"
      min_lines: 20
    - path: "lib/safe-action.ts"
      provides: "next-safe-action client configured with Supabase auth"
      min_lines: 10
    - path: "components/delete-confirm-dialog.tsx"
      provides: "Type-name-to-confirm delete dialog"
      min_lines: 30
  key_links:
    - from: "app/(dashboard)/admin/settings/page.tsx"
      to: "nuqs"
      via: "useQueryState for tab parameter"
      pattern: "useQueryState.*tab"
    - from: "components/data-table/data-table.tsx"
      to: "@tanstack/react-table"
      via: "useReactTable hook"
      pattern: "useReactTable"
    - from: "components/sidebar.tsx"
      to: "app/(dashboard)/admin/settings/page.tsx"
      via: "nav item href /admin/settings with built: true"
      pattern: "built:\\s*true"
---

<objective>
Install shadcn/ui component library and all Phase 3 dependencies, create reusable data table infrastructure (sorting, filtering, pagination, bulk actions), build the Settings page shell with tab navigation, create shared UI primitives (delete confirmation dialog, inline feedback), and update the sidebar to show admin nav items as active.

Purpose: Establish the UI foundation that all CRUD pages in plans 03-02 and 03-03 will build on. Without these reusable components and the page shell, the entity-specific CRUD implementations cannot proceed.

Output: Installable shadcn/ui components, reusable DataTable component, Settings page with tabs, updated sidebar navigation, safe-action client, shared dialog primitives.

**ROADMAP note:** ROADMAP success criteria #4 ("name, avatar, notification preferences") and #5 ("toast notifications") will be updated after phase execution to match user LOCKED decisions: name-only editable profile with auto-generated initials avatar (no upload), no notification preferences, inline feedback (not toasts).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-auth-rbac/02-02-SUMMARY.md
@.planning/phases/03-admin-system-configuration/03-RESEARCH.md
@.planning/phases/03-admin-system-configuration/03-CONTEXT.md

Key existing files to understand:
@components/sidebar.tsx
@components/user-menu.tsx
@app/(dashboard)/layout.tsx
@lib/auth/permissions.ts
@lib/auth/hooks.tsx
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn/ui and all Phase 3 dependencies</name>
  <files>
    package.json
    components.json
    components/ui/*.tsx
    lib/utils.ts
    app/globals.css
    lib/safe-action.ts
  </files>
  <action>
1. Initialize shadcn/ui in the project:
   ```bash
   npx shadcn@latest init --defaults
   ```
   This creates `components.json`, `lib/utils.ts` (with `cn` helper), and updates `app/globals.css` with CSS variables. If prompted for style, choose "new-york". Use the default Tailwind v4 detection.

   **IMPORTANT:** After init, verify that `app/globals.css` still has the existing `@import "tailwindcss"` line and any existing CSS custom properties from Phase 2 are preserved. If shadcn/ui overwrites globals.css, merge the content to keep both the shadcn/ui theme variables AND the existing Tailwind import.

2. Install shadcn/ui components (all needed for Phase 3):
   ```bash
   npx shadcn@latest add table dialog form input button select tabs sheet alert-dialog checkbox dropdown-menu label badge separator
   ```
   These create files in `components/ui/`. Verify each component file exists after installation.

3. Install npm dependencies for Phase 3:
   ```bash
   npm install @tanstack/react-table react-hook-form @hookform/resolvers zod next-safe-action nuqs date-fns lucide-react
   ```

4. Create the next-safe-action client at `lib/safe-action.ts`:
   ```typescript
   import { createSafeActionClient } from "next-safe-action";
   import { createClient } from "@/lib/supabase/server";

   export const actionClient = createSafeActionClient({
     handleServerError(e) {
       console.error("Action error:", e.message);
       return e.message;
     },
   });

   // Authenticated action client - checks auth and provides supabase + user
   export const authActionClient = actionClient.use(async ({ next }) => {
     const supabase = await createClient();
     const { data: { user }, error } = await supabase.auth.getUser();

     if (error || !user) {
       throw new Error("Unauthorized");
     }

     // Fetch profile for role checking
     const { data: profile } = await supabase
       .from("user_profiles")
       .select("*")
       .eq("id", user.id)
       .single();

     if (!profile || profile.deleted_at) {
       throw new Error("Account deactivated");
     }

     return next({
       ctx: { supabase, user, profile },
     });
   });

   // Admin-only action client
   export const adminActionClient = authActionClient.use(async ({ ctx, next }) => {
     if (ctx.profile.role !== "admin") {
       throw new Error("Admin access required");
     }
     return next({ ctx });
   });
   ```

5. Verify installation:
   - `npm run build` should pass (or at minimum `npx tsc --noEmit` should pass)
   - Check that `components/ui/table.tsx`, `components/ui/dialog.tsx`, `components/ui/tabs.tsx` etc. all exist
   - Check that `lib/utils.ts` exports `cn` function
  </action>
  <verify>
    - `ls components/ui/` shows table.tsx, dialog.tsx, form.tsx, input.tsx, button.tsx, select.tsx, tabs.tsx, sheet.tsx, alert-dialog.tsx, checkbox.tsx, dropdown-menu.tsx, label.tsx, badge.tsx, separator.tsx
    - `cat lib/utils.ts` shows cn function
    - `cat lib/safe-action.ts` shows actionClient and adminActionClient exports
    - `npx tsc --noEmit` passes
    - `npm run lint` passes (or only pre-existing warnings)
  </verify>
  <done>
    shadcn/ui initialized with all 14 required components installed, TanStack Table + react-hook-form + zod + next-safe-action + nuqs + date-fns + lucide-react installed, safe-action client with auth and admin middleware layers created, TypeScript compilation passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Create reusable data table components and shared UI primitives</name>
  <files>
    components/data-table/data-table.tsx
    components/data-table/data-table-pagination.tsx
    components/data-table/data-table-toolbar.tsx
    components/data-table/data-table-column-header.tsx
    components/delete-confirm-dialog.tsx
    components/inline-feedback.tsx
  </files>
  <action>
1. Create `components/data-table/data-table.tsx` — the core reusable DataTable component:
   - Generic TypeScript component: `DataTable<TData, TValue>`
   - Props: `columns: ColumnDef<TData, TValue>[]`, `data: TData[]`, `searchKey?: string` (column key for search input), `filterableColumns?: { id: string; title: string; options: { label: string; value: string }[] }[]`, `onBulkDelete?: (ids: string[]) => Promise<void>`, `onBulkExport?: (ids: string[]) => void`, `showDeactivatedToggle?: boolean`, `onDeactivatedToggleChange?: (show: boolean) => void`, `showDeactivated?: boolean`, `pageSize?: number` (default 10)
   - Uses `useReactTable` with: getCoreRowModel, getPaginationRowModel, getSortedRowModel, getFilteredRowModel
   - State: sorting, columnFilters, columnVisibility, rowSelection, pagination (pageIndex, pageSize)
   - Renders: DataTableToolbar above, shadcn/ui Table in middle, DataTablePagination below
   - **CRITICAL:** Define columns and data references properly to avoid infinite re-render loops (see research pitfall #1). The component receives columns/data as props which are stable references from parent.
   - Uses flexRender for cell rendering
   - Empty state: When data.length === 0 and no active filters, show centered "No [items] found" text with no illustration (per user decision: minimal text + create button placeholder)

2. Create `components/data-table/data-table-toolbar.tsx`:
   - Props: `table: Table<TData>`, `searchKey?: string`, `filterableColumns?: ...`, `selectedRowCount: number`, `onBulkDelete?`, `onBulkExport?`, `showDeactivatedToggle?: boolean`, `onDeactivatedToggleChange?`, `showDeactivated?: boolean`, `createButton?: ReactNode`
   - Layout: Horizontal flex bar, always visible above table
   - Left side: Search Input (filters on searchKey column), filter dropdowns (Select component for each filterable column), deactivated toggle (Checkbox + label "Show deactivated")
   - Right side: When selectedRowCount > 0, show "{N} selected" text + "Export" button (if onBulkExport provided) + "Delete" destructive button (if onBulkDelete provided). When no selection, show the createButton ReactNode.
   - Search uses `table.getColumn(searchKey)?.setFilterValue(value)` for client-side filtering

3. Create `components/data-table/data-table-pagination.tsx`:
   - Props: `table: Table<TData>`, `pageSizeOptions?: number[]` (default [10, 25, 50])
   - Layout: Flex row with "Showing X of Y rows" on left, page size selector (Select), page number buttons in center, Previous/Next on right
   - Page numbers: Show first, last, current, and adjacent pages with ellipsis (e.g., 1 ... 4 5 6 ... 12)
   - Uses `table.setPageSize()`, `table.setPageIndex()`, `table.getCanPreviousPage()`, `table.getCanNextPage()`

4. Create `components/data-table/data-table-column-header.tsx`:
   - Props: `column: Column<TData, TValue>`, `title: string`, `className?: string`
   - Renders column title with sort indicator (up/down/none arrows)
   - Clicking toggles sorting via `column.toggleSorting()`
   - Uses `ArrowUpDown`, `ArrowUp`, `ArrowDown` icons from lucide-react

5. Create `components/delete-confirm-dialog.tsx`:
   - Props: `open: boolean`, `onOpenChange: (open: boolean) => void`, `entityName: string`, `entityType: string`, `onConfirm: () => Promise<void>`, `dependencyCount?: number`, `dependencyLabel?: string`
   - Uses shadcn/ui AlertDialog
   - If dependencyCount > 0: show error message "Cannot delete -- {dependencyCount} {dependencyLabel} assigned" and disable the confirm button entirely. No text input shown.
   - If dependencyCount === 0 or undefined: show text input where user must type the entity name exactly to enable the delete button
   - Confirm button text: "Delete" (destructive red styling), disabled until input matches entityName
   - Loading state: button shows "Deleting..." and disables while onConfirm promise is pending
   - On successful confirm: calls onOpenChange(false) and resets input

6. Create `components/inline-feedback.tsx`:
   - Simple component/utility for inline success messages near actions
   - Props: `message: string | null`, `type: 'success' | 'error'`
   - Renders a small text message with green (success) or red (error) color
   - Auto-fades after 3 seconds using CSS animation
   - Also export a CSS class `.row-highlight` with a brief green flash keyframe animation for table rows (add to globals.css or as a CSS module)
   - This is kept minimal per user decision: inline feedback, not toasts
  </action>
  <verify>
    - `ls components/data-table/` shows data-table.tsx, data-table-pagination.tsx, data-table-toolbar.tsx, data-table-column-header.tsx
    - `ls components/delete-confirm-dialog.tsx` exists
    - `ls components/inline-feedback.tsx` exists
    - `npx tsc --noEmit` passes
    - All components import from `@/components/ui/*` (shadcn/ui) and `@tanstack/react-table`
  </verify>
  <done>
    Reusable DataTable with sorting/filtering/pagination/bulk-actions renders without errors, DeleteConfirmDialog with type-to-confirm and dependency blocking works, InlineFeedback component provides non-toast action feedback, all TypeScript compilation passes
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Settings page with tabs, admin layout, and update sidebar</name>
  <files>
    app/(dashboard)/admin/layout.tsx
    app/(dashboard)/admin/settings/page.tsx
    components/sidebar.tsx
    components/user-menu.tsx
    app/(dashboard)/layout.tsx
  </files>
  <action>
1. Create `app/(dashboard)/admin/layout.tsx` — admin permission gate wrapper:
   - Server component that checks user profile role
   - If role !== 'admin', redirect to `/unauthorized`
   - Otherwise render children
   - This protects both /admin/settings and /admin/users routes
   - Uses `createClient` from `@/lib/supabase/server` to fetch user and profile server-side
   - Pattern: same auth check as dashboard layout but with admin role gate

2. Create `app/(dashboard)/admin/settings/page.tsx` — Settings page with tab navigation:
   - **Must be a client component** (`'use client'`) because it uses nuqs for URL state
   - Import: `useQueryState` from `nuqs` for tab state management
   - Wrap the page in `NuqsAdapter` from `nuqs/adapters/next/app` if required by nuqs for Next.js App Router
   - Four main tabs: "Companies", "Divisions", "Locations", "Categories"
   - The "Categories" tab has two sub-tabs: "Request Categories" and "Asset Categories"
   - Tab state stored in URL: `?tab=companies` (default), `?tab=divisions`, `?tab=locations`, `?tab=categories`
   - Category sub-tab state: `?categoryType=request` (default), `?categoryType=asset`
   - Use shadcn/ui `Tabs`, `TabsList`, `TabsTrigger`, `TabsContent` components
   - Each TabsContent renders a placeholder div with text like "Companies table coming in next plan" (these will be replaced in 03-02)
   - Page title: "Settings" with subtitle "Manage your organization's configuration"
   - Layout: Full width, adequate spacing

3. Update `components/sidebar.tsx`:
   - Change the Admin section nav items:
     - "Users" item: change `href` from `/admin/users` to `/admin/users`, keep same. Change `built: false` to `built: true`
     - "Company Settings" item: change `label` to "Settings", `href` from `/admin/company` to `/admin/settings`, change `built: false` to `built: true`
   - **Entity count badges:** Add optional `badge?: number | null` to `NavItem` type
     - The sidebar receives counts from the dashboard layout (passed as prop)
     - Add a new prop to Sidebar: `entityCounts?: { divisions?: number; locations?: number; categories?: number; users?: number }`
     - Render badge next to label for Settings/Users items showing counts (e.g., show nothing for now — the actual count fetching will be wired when entities have data)
     - For now, don't fetch counts yet (that requires Supabase queries in the layout). Just add the badge rendering capability. The counts prop will be optional and only show badges when provided.
   - Ensure only admin-role users see the Admin section (already handled by hasPermission filter)

4. Update `components/user-menu.tsx`:
   - Change the "Profile" link from `<Link href="/profile">` to a button/trigger that will open the Profile Sheet
   - For now, keep it as-is but change href to "#" and add a `data-profile-trigger` attribute (the actual Sheet integration happens in 03-03)
   - Change the "Settings" link to point to `/admin/settings` instead of `/settings`
   - Only show "Settings" link if user role is admin (check using usePermission hook with PERMISSIONS.ADMIN_PANEL)

5. Update `app/(dashboard)/layout.tsx`:
   - Add entity count queries to fetch from Supabase:
     ```typescript
     const [divisionsCount, locationsCount, categoriesCount, usersCount] = await Promise.all([
       supabase.from('divisions').select('*', { count: 'exact', head: true }).is('deleted_at', null),
       supabase.from('locations').select('*', { count: 'exact', head: true }).is('deleted_at', null),
       supabase.from('categories').select('*', { count: 'exact', head: true }).is('deleted_at', null),
       supabase.from('user_profiles').select('*', { count: 'exact', head: true }).is('deleted_at', null),
     ]);
     ```
   - Pass `entityCounts` prop to Sidebar component
   - Only pass counts if profile.role === 'admin' (non-admin users don't see the Admin section)

6. Verify:
   - `npm run build` passes
   - `npm run lint` passes (or only pre-existing warnings)
   - Visit `/admin/settings` renders the tabbed Settings page
   - Sidebar shows "Settings" and "Users" as active links for admin role
   - Tab switching updates URL query parameter
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - `npm run build` passes
    - `npm run lint` passes
    - `cat app/(dashboard)/admin/settings/page.tsx` shows Tabs with Companies/Divisions/Locations/Categories
    - `cat app/(dashboard)/admin/layout.tsx` shows admin role check
    - `grep "built: true" components/sidebar.tsx` shows Settings and Users items
    - `cat components/user-menu.tsx` shows conditional Settings link for admin
  </verify>
  <done>
    Settings page renders at /admin/settings with four tabs controlled by URL query parameter, admin layout gates access to admin-only users, sidebar shows Settings and Users as active clickable links with badge rendering capability, user menu links updated, entity count queries wired in dashboard layout
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `npx tsc --noEmit` passes
3. `npm run lint` passes
4. shadcn/ui components are importable: `components/ui/table.tsx`, `components/ui/dialog.tsx`, `components/ui/tabs.tsx` exist
5. Visit `/admin/settings` shows tabbed Settings page with four tabs
6. Clicking tabs updates URL query parameter (?tab=divisions, etc.)
7. Sidebar shows "Settings" and "Users" as clickable links (not "Coming soon") for admin users
8. Non-admin users are redirected from /admin/* routes to /unauthorized
9. DataTable component exists at `components/data-table/data-table.tsx` with TanStack Table integration
10. DeleteConfirmDialog component exists with type-to-confirm pattern
</verification>

<success_criteria>
- All shadcn/ui components installed and TypeScript-compatible
- Reusable DataTable component with sorting, filtering, pagination, row selection, bulk actions
- Settings page at /admin/settings with working tab navigation (URL-synced)
- Admin layout with permission gate protecting all /admin/* routes
- Sidebar updated with active Settings and Users links for admin role
- Entity count badge rendering capability in sidebar
- safe-action client configured with auth and admin middleware
- DeleteConfirmDialog with type-to-confirm and dependency blocking
- InlineFeedback component for non-toast action feedback
- Build, TypeScript, and lint all pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-admin-system-configuration/03-01-SUMMARY.md`
</output>
