---
phase: 09-polish-integration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - components/ui/button.tsx
  - components/ui/input.tsx
  - components/ui/textarea.tsx
  - components/ui/badge.tsx
  - components/ui/checkbox.tsx
  - components/ui/select.tsx
  - components/ui/tabs.tsx
  - components/ui/dropdown-menu.tsx
  - components/sidebar.tsx
  - components/user-menu.tsx
  - components/inline-feedback.tsx
  - components/profile/profile-sheet.tsx
  - components/requests/request-timeline.tsx
  - components/requests/request-triage-dialog.tsx
  - components/admin/users/user-columns.tsx
  - components/admin/users/user-form-dialog.tsx
  - lib/constants/request-status.ts
  - app/(auth)/login/page.tsx
  - app/(auth)/update-password/page.tsx
  - app/(auth)/reset-password/page.tsx
  - app/(dashboard)/page.tsx
  - app/(dashboard)/layout.tsx
  - app/(dashboard)/requests/[id]/page.tsx
  - app/(dashboard)/admin/users/page.tsx
  - app/unauthorized/page.tsx
  - components/skeletons/dashboard-skeleton.tsx
  - components/skeletons/request-list-skeleton.tsx
  - components/skeletons/request-detail-skeleton.tsx
  - components/skeletons/settings-skeleton.tsx
  - components/skeletons/users-skeleton.tsx
  - components/skeletons/request-new-skeleton.tsx
  - app/(dashboard)/loading.tsx
  - app/(dashboard)/requests/loading.tsx
  - app/(dashboard)/requests/[id]/loading.tsx
  - app/(dashboard)/requests/new/loading.tsx
  - app/(dashboard)/admin/settings/loading.tsx
  - app/(dashboard)/admin/users/loading.tsx
autonomous: true
requirements:
  - REQ-UI-003
  - REQ-UI-004

must_haves:
  truths:
    - "No dark: Tailwind classes exist anywhere in the codebase"
    - "No .dark CSS block or @custom-variant dark exists in globals.css"
    - "Every data-loading page has a custom loading skeleton matching its layout"
    - "Loading skeletons display while server components fetch data"
  artifacts:
    - path: "app/globals.css"
      provides: "Clean CSS without dark mode variables or custom variant"
    - path: "app/(dashboard)/loading.tsx"
      provides: "Dashboard page loading skeleton"
    - path: "app/(dashboard)/requests/loading.tsx"
      provides: "Request list page loading skeleton"
    - path: "app/(dashboard)/requests/[id]/loading.tsx"
      provides: "Request detail page loading skeleton"
    - path: "app/(dashboard)/admin/settings/loading.tsx"
      provides: "Settings page loading skeleton"
    - path: "app/(dashboard)/admin/users/loading.tsx"
      provides: "Users page loading skeleton"
    - path: "components/skeletons/dashboard-skeleton.tsx"
      provides: "Custom skeleton component for dashboard page"
      exports: ["DashboardSkeleton"]
    - path: "components/skeletons/request-list-skeleton.tsx"
      provides: "Custom skeleton component for request list page"
      exports: ["RequestListSkeleton"]
  key_links:
    - from: "app/(dashboard)/loading.tsx"
      to: "components/skeletons/dashboard-skeleton.tsx"
      via: "Import and render skeleton component"
      pattern: "DashboardSkeleton"
    - from: "app/(dashboard)/requests/loading.tsx"
      to: "components/skeletons/request-list-skeleton.tsx"
      via: "Import and render skeleton component"
      pattern: "RequestListSkeleton"
---

<objective>
Remove all dark mode remnants from the codebase and add custom loading skeletons for all data-loading pages.

Purpose: REQ-UI-003 is DROPPED (no dark mode). Existing dark: classes are dead code that should be cleaned up. REQ-UI-004 requires loading skeletons on all data-loading pages to improve perceived performance. Each page gets a custom skeleton matching its exact layout.

Output: Clean codebase with no dark mode references. Custom skeleton components and loading.tsx files for all existing data pages.
</objective>

<execution_context>
@/Users/melfice/.claude/get-shit-done/workflows/execute-plan.md
@/Users/melfice/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-polish-integration/09-CONTEXT.md
@.planning/phases/09-polish-integration/09-RESEARCH.md

# Reference files
@app/globals.css
@app/(dashboard)/page.tsx
@app/(dashboard)/requests/page.tsx
@app/(dashboard)/requests/[id]/page.tsx
@app/(dashboard)/admin/settings/page.tsx
@app/(dashboard)/admin/users/page.tsx
@components/ui/skeleton.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove all dark mode remnants from the codebase</name>
  <files>
    app/globals.css
    components/ui/button.tsx
    components/ui/input.tsx
    components/ui/textarea.tsx
    components/ui/badge.tsx
    components/ui/checkbox.tsx
    components/ui/select.tsx
    components/ui/tabs.tsx
    components/ui/dropdown-menu.tsx
    components/sidebar.tsx
    components/user-menu.tsx
    components/inline-feedback.tsx
    components/profile/profile-sheet.tsx
    components/requests/request-timeline.tsx
    components/requests/request-triage-dialog.tsx
    components/admin/users/user-columns.tsx
    components/admin/users/user-form-dialog.tsx
    lib/constants/request-status.ts
    app/(auth)/login/page.tsx
    app/(auth)/update-password/page.tsx
    app/(auth)/reset-password/page.tsx
    app/(dashboard)/page.tsx
    app/(dashboard)/layout.tsx
    app/(dashboard)/requests/[id]/page.tsx
    app/(dashboard)/admin/users/page.tsx
    app/unauthorized/page.tsx
  </files>
  <action>
**CRITICAL: This is a mechanical cleanup. Be methodical and test after each file.**

**Step 1: globals.css cleanup**
- Remove the entire `.dark { ... }` block (lines 85-117 approximately) — this contains all dark mode CSS custom property overrides
- Remove the `@custom-variant dark (&:is(.dark *));` line (line 5)
- Keep everything else (`:root` block, `@layer base`, animations, etc.) unchanged
- The `:root` light mode values are the ONLY values needed

**Step 2: Remove dark: classes from all 25 files (140 occurrences)**
For each file listed, remove all `dark:*` Tailwind class references. The approach:
- For inline className strings like `"bg-white dark:bg-gray-950"`, remove the `dark:bg-gray-950` part, keeping `"bg-white"`
- For conditional dark classes in template literals or arrays, remove the dark variant
- Be careful NOT to remove the non-dark class that precedes or follows the dark: class
- In shadcn UI components (button, input, badge, etc.), the `dark:` classes are in the component's own className. Remove them but verify the light mode classes still look correct

**File-by-file specifics (based on grep results):**

1. `app/(dashboard)/layout.tsx` (1 occurrence): Remove `dark:bg-gray-950` from main element
2. `app/(dashboard)/page.tsx` (18 occurrences): Remove all `dark:*` classes from dashboard page
3. `app/(auth)/login/page.tsx` (18 occurrences): Remove all `dark:*` classes from login page
4. `app/(auth)/update-password/page.tsx` (14 occurrences): Remove all `dark:*` classes
5. `app/(auth)/reset-password/page.tsx` (9 occurrences): Remove all `dark:*` classes
6. `app/(dashboard)/requests/[id]/page.tsx` (3 occurrences): Remove dark classes
7. `app/(dashboard)/admin/users/page.tsx` (1 occurrence): Remove dark class
8. `app/unauthorized/page.tsx` (5 occurrences): Remove dark classes
9. `components/sidebar.tsx` (9 occurrences): Remove dark classes from sidebar styles
10. `components/user-menu.tsx` (13 occurrences): Remove dark classes
11. `components/profile/profile-sheet.tsx` (6 occurrences): Remove dark classes
12. `components/inline-feedback.tsx` (2 occurrences): Remove dark classes
13. `components/requests/request-timeline.tsx` (7 occurrences): Remove dark classes
14. `components/requests/request-triage-dialog.tsx` (1 occurrence): Remove dark class
15. `components/admin/users/user-columns.tsx` (6 occurrences): Remove dark classes
16. `components/admin/users/user-form-dialog.tsx` (1 occurrence): Remove dark class
17. `lib/constants/request-status.ts` (11 occurrences): Remove dark classes from status/priority color mappings
18. `components/ui/button.tsx` (4 occurrences): Remove dark variant classes
19. `components/ui/input.tsx` (2 occurrences): Remove dark variant classes
20. `components/ui/textarea.tsx` (1 occurrence): Remove dark variant class
21. `components/ui/badge.tsx` (2 occurrences): Remove dark variant classes
22. `components/ui/checkbox.tsx` (1 occurrence): Remove dark variant class
23. `components/ui/select.tsx` (1 occurrence): Remove dark variant class
24. `components/ui/tabs.tsx` (3 occurrences): Remove dark variant classes
25. `components/ui/dropdown-menu.tsx` (1 occurrence): Remove dark variant class

**Step 3: Verify no dark: classes remain**
After all edits, run: `grep -r "dark:" --include="*.tsx" --include="*.ts" --include="*.css" . | grep -v node_modules | grep -v .next`
The result should be empty (zero matches).

**Step 4: Build check**
Run `npm run build` to verify nothing is broken. The light mode styles should look identical since `dark:` classes only applied in dark mode.
  </action>
  <verify>
    <automated>cd /Users/melfice/code/ga && grep -r "dark:" --include="*.tsx" --include="*.ts" --include="*.css" . | grep -v node_modules | grep -v .next | wc -l</automated>
    <manual>Verify the application looks correct in the browser (light mode unchanged)</manual>
  </verify>
  <done>Zero `dark:` classes exist in the codebase. The `.dark` CSS block and `@custom-variant dark` are removed from globals.css. Application renders correctly in light mode only. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create custom loading skeletons for all data-loading pages</name>
  <files>
    components/skeletons/dashboard-skeleton.tsx
    components/skeletons/request-list-skeleton.tsx
    components/skeletons/request-detail-skeleton.tsx
    components/skeletons/settings-skeleton.tsx
    components/skeletons/users-skeleton.tsx
    components/skeletons/request-new-skeleton.tsx
    app/(dashboard)/loading.tsx
    app/(dashboard)/requests/loading.tsx
    app/(dashboard)/requests/[id]/loading.tsx
    app/(dashboard)/requests/new/loading.tsx
    app/(dashboard)/admin/settings/loading.tsx
    app/(dashboard)/admin/users/loading.tsx
  </files>
  <action>
Create the `components/skeletons/` directory.

**Each skeleton component must mirror the exact layout of its corresponding page.** Read each page.tsx to understand its layout before creating the skeleton.

**1. DashboardSkeleton (components/skeletons/dashboard-skeleton.tsx):**
Mirror the dashboard page layout:
- Welcome heading skeleton (h-8 w-64)
- User info text skeleton (h-4 w-48)
- Stats cards area: 3-4 skeleton cards in a grid (matching the dashboard's KPI layout if it exists, otherwise a simple grid of 3 card-shaped skeletons)
- Each card: rounded border, h-24, with title skeleton (h-4 w-20) and value skeleton (h-8 w-16)
- Use shadcn `Skeleton` component from `@/components/ui/skeleton`

**2. RequestListSkeleton (components/skeletons/request-list-skeleton.tsx):**
Mirror the request list page:
- Page header: title skeleton (h-8 w-48) + description (h-4 w-80 mt-2) + "New Request" button skeleton (h-9 w-32)
- Filter toolbar: row of 4-5 filter input skeletons (h-8 w-[180px] each) with gap-2
- Data table: rounded border container with table header row (5 column header skeletons h-4 w-various) and 8 data rows (5 cell skeletons per row with varying widths, badges as rounded-full shapes)
- Pagination: row with "showing X of Y" skeleton left, page buttons right

**3. RequestDetailSkeleton (components/skeletons/request-detail-skeleton.tsx):**
Mirror the request detail two-column layout:
- Breadcrumb skeleton at top (3 items with separators)
- Two-column grid: `grid-cols-[1fr_380px] max-lg:grid-cols-1 gap-6`
- Left column (info): Section with multiple field rows (label skeleton h-4 w-24, value skeleton h-4 w-48), photo grid area (3 image placeholder skeletons h-24 w-24 each)
- Right column (timeline): vertical list of 5 timeline entry skeletons (icon circle h-8 w-8, text lines h-4 w-various, timestamp h-3 w-28)

**4. SettingsSkeleton (components/skeletons/settings-skeleton.tsx):**
Mirror the settings page:
- Page header: title skeleton (h-8 w-36)
- Tab bar: row of 4 tab skeletons (h-9 w-24 each)
- Table area (below tabs): search input skeleton (h-8 w-[250px]) + "Add" button skeleton (h-9 w-24), then data table skeleton with 4 columns and 6 rows

**5. UsersSkeleton (components/skeletons/users-skeleton.tsx):**
Mirror the users page:
- Page header: title skeleton (h-8 w-32) + company selector skeleton (h-9 w-48) + "Add User" button skeleton (h-9 w-28)
- Data table: 6 columns (name, email, role, division, status, actions) with 6 row skeletons

**6. RequestNewSkeleton (components/skeletons/request-new-skeleton.tsx):**
Mirror the new request form:
- Breadcrumb skeleton at top
- Card container with form skeletons: description textarea skeleton (h-24 w-full), location dropdown skeleton (h-9 w-full), photo upload area skeleton (h-32 w-full dashed border), submit button skeleton (h-9 w-32)

**Loading.tsx files:**
Each `loading.tsx` file is minimal — just imports and renders the corresponding skeleton:

```tsx
import { RequestListSkeleton } from '@/components/skeletons/request-list-skeleton';

export default function Loading() {
  return <RequestListSkeleton />;
}
```

Create loading.tsx for:
- `app/(dashboard)/loading.tsx` -> DashboardSkeleton
- `app/(dashboard)/requests/loading.tsx` -> RequestListSkeleton
- `app/(dashboard)/requests/[id]/loading.tsx` -> RequestDetailSkeleton
- `app/(dashboard)/requests/new/loading.tsx` -> RequestNewSkeleton
- `app/(dashboard)/admin/settings/loading.tsx` -> SettingsSkeleton
- `app/(dashboard)/admin/users/loading.tsx` -> UsersSkeleton

**IMPORTANT:** For any pages from Phases 5-8 (jobs, inventory, maintenance, approvals, notifications), create loading.tsx files ONLY if those route directories exist at execution time. Do NOT create directories for pages that don't exist yet.
  </action>
  <verify>
    <automated>cd /Users/melfice/code/ga && ls components/skeletons/*.tsx && ls app/\(dashboard\)/loading.tsx app/\(dashboard\)/requests/loading.tsx app/\(dashboard\)/requests/\[id\]/loading.tsx app/\(dashboard\)/admin/settings/loading.tsx app/\(dashboard\)/admin/users/loading.tsx && npm run build 2>&1 | tail -5</automated>
    <manual>Navigate between pages and verify skeleton loading states appear during data fetch</manual>
  </verify>
  <done>Six custom skeleton components exist, each matching the exact layout of its corresponding page. Six loading.tsx files render these skeletons during server-side data fetching. Build passes.</done>
</task>

</tasks>

<verification>
1. `grep -r "dark:" . --include="*.tsx" --include="*.ts" --include="*.css" | grep -v node_modules | grep -v .next` returns 0 results
2. `app/globals.css` has no `.dark` block and no `@custom-variant dark`
3. All 6 loading.tsx files exist in correct route directories
4. All 6 skeleton components exist in `components/skeletons/`
5. `npm run build` passes
</verification>

<success_criteria>
- Zero dark: classes in the codebase (REQ-UI-003 cleanup complete)
- globals.css contains only light mode CSS custom properties
- Every existing data-loading page has a custom loading skeleton
- Skeletons match the exact layout of their corresponding pages
- Application renders correctly with no visual regressions
</success_criteria>

<output>
After completion, create `.planning/phases/09-polish-integration/09-03-SUMMARY.md`
</output>
