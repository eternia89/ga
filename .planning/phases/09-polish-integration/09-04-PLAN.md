---
phase: 09-polish-integration
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(dashboard)/layout.tsx
  - components/sidebar.tsx
  - components/mobile-menu.tsx
  - components/requests/request-photo-upload.tsx
  - components/requests/request-submit-form.tsx
  - components/requests/request-edit-form.tsx
  - components/requests/request-photo-lightbox.tsx
  - components/admin/categories/category-form-dialog.tsx
  - components/admin/companies/company-form-dialog.tsx
  - components/admin/divisions/division-form-dialog.tsx
  - components/admin/locations/location-form-dialog.tsx
  - components/admin/users/user-form-dialog.tsx
  - components/profile/profile-sheet.tsx
  - components/data-table/data-table.tsx
  - components/data-table/data-table-pagination.tsx
  - components/data-table/data-table-toolbar.tsx
  - app/(dashboard)/page.tsx
  - app/(dashboard)/requests/page.tsx
  - app/(dashboard)/requests/new/page.tsx
  - app/(dashboard)/requests/[id]/page.tsx
  - app/(dashboard)/admin/settings/page.tsx
  - app/(dashboard)/admin/users/page.tsx
  - app/(auth)/login/page.tsx
  - app/(auth)/reset-password/page.tsx
  - app/(auth)/update-password/page.tsx
  - app/not-found.tsx
  - app/unauthorized/page.tsx
autonomous: false
requirements:
  - REQ-UI-006
  - REQ-UI-007

must_haves:
  truths:
    - "Sidebar collapses to a hamburger menu on mobile viewports"
    - "All form dialogs become full-screen sheets on mobile"
    - "Photo upload on mobile uses native camera capture"
    - "Tables are horizontally scrollable on mobile"
    - "Full path breadcrumbs are present on all interior pages"
    - "All pages are usable on mobile devices"
    - "Error pages (404, unauthorized) have consistent styling"
  artifacts:
    - path: "components/mobile-menu.tsx"
      provides: "Hamburger menu button and Sheet-based mobile sidebar"
      exports: ["MobileMenu"]
    - path: "app/not-found.tsx"
      provides: "Consistent 404 error page"
    - path: "app/(dashboard)/requests/page.tsx"
      provides: "Request list with breadcrumb navigation"
      contains: "Breadcrumb"
  key_links:
    - from: "app/(dashboard)/layout.tsx"
      to: "components/mobile-menu.tsx"
      via: "Conditional render for mobile sidebar"
      pattern: "MobileMenu"
    - from: "components/mobile-menu.tsx"
      to: "components/sidebar.tsx"
      via: "Renders Sidebar inside Sheet on mobile"
      pattern: "Sheet.*Sidebar"
    - from: "components/requests/request-photo-upload.tsx"
      to: "native camera"
      via: "capture attribute on file input for mobile"
      pattern: "capture.*environment"
---

<objective>
Make the application mobile-responsive for field workers, add breadcrumb navigation to all interior pages, and perform a UI consistency pass.

Purpose: REQ-UI-007 requires mobile responsiveness especially for field workflows (job status updates, photo uploads). REQ-UI-006 requires full-path breadcrumbs on all interior pages. This plan also standardizes error pages and ensures UI consistency across all existing pages.

Output: Hamburger menu sidebar for mobile, responsive layouts on all pages, camera capture for mobile photo uploads, breadcrumbs on all interior pages, standardized error pages, consistent UI across the application.
</objective>

<execution_context>
@/Users/melfice/.claude/get-shit-done/workflows/execute-plan.md
@/Users/melfice/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-polish-integration/09-CONTEXT.md
@.planning/phases/09-polish-integration/09-RESEARCH.md

# Reference files
@app/(dashboard)/layout.tsx
@components/sidebar.tsx
@app/(dashboard)/requests/[id]/page.tsx
@components/requests/request-photo-upload.tsx
@components/ui/sheet.tsx
@components/ui/breadcrumb.tsx
@components/data-table/data-table.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mobile sidebar hamburger menu, responsive layouts, and camera capture</name>
  <files>
    components/mobile-menu.tsx
    app/(dashboard)/layout.tsx
    components/sidebar.tsx
    components/requests/request-photo-upload.tsx
    components/requests/request-submit-form.tsx
    components/requests/request-edit-form.tsx
    components/requests/request-photo-lightbox.tsx
    components/admin/categories/category-form-dialog.tsx
    components/admin/companies/company-form-dialog.tsx
    components/admin/divisions/division-form-dialog.tsx
    components/admin/locations/location-form-dialog.tsx
    components/admin/users/user-form-dialog.tsx
    components/profile/profile-sheet.tsx
    components/data-table/data-table.tsx
    components/data-table/data-table-pagination.tsx
    components/data-table/data-table-toolbar.tsx
  </files>
  <action>
**1. Mobile sidebar hamburger (components/mobile-menu.tsx):**
Create a new `'use client'` component `MobileMenu`:
- Accepts `companyName: string` prop (same as Sidebar)
- Uses shadcn Sheet component with `side="left"` and `className="w-64 p-0"`
- Trigger: A Button with `variant="ghost"` and `size="icon"` containing a `Menu` icon (lucide-react)
- Sheet content: Renders the existing `<Sidebar companyName={companyName} />` component inside SheetContent
- When a navigation link is clicked inside the sidebar in mobile mode, the sheet should close. Use Sheet's `onOpenChange` and pass an `onNavigate` callback to Sidebar, or use `usePathname` to detect navigation and auto-close.
- SheetTitle for accessibility: "Navigation" (visually hidden with sr-only)

**2. Dashboard layout update (app/(dashboard)/layout.tsx):**
Modify the layout to support mobile:
```
<div className="flex h-screen">
  {/* Desktop sidebar - hidden on mobile */}
  <div className="max-md:hidden">
    <Sidebar companyName={companyName} />
  </div>

  <div className="flex-1 flex flex-col overflow-hidden">
    {/* Mobile header with hamburger - hidden on desktop */}
    <div className="hidden max-md:flex items-center gap-3 border-b px-4 py-3">
      <MobileMenu companyName={companyName} />
      <span className="text-sm font-semibold">{companyName}</span>
    </div>

    <main className="flex-1 overflow-auto p-6 max-md:p-4 bg-white">
      {children}
    </main>
  </div>
</div>
```
- Remove `dark:bg-gray-950` from main if Task 1 of Plan 03 hasn't run yet (it may already be removed)
- Add `'use client'` directive since MobileMenu is a client component — OR keep layout as server component and just import MobileMenu as a client island. The latter is preferred.

**3. Sidebar updates (components/sidebar.tsx):**
- If Sidebar needs to communicate "close sheet" to MobileMenu, add an optional `onNavigate?: () => void` prop
- Call `onNavigate` when any nav link is clicked (in Link onClick or via usePathname effect)
- No layout changes needed — Sidebar already has the correct desktop layout

**4. Photo upload mobile camera (components/requests/request-photo-upload.tsx):**
Modify the file input for photo uploads:
- Add `capture="environment"` attribute ONLY on mobile. Use a conditional approach:
  - Render two file inputs: one with `capture="environment"` shown only on mobile (`hidden max-md:block`), one without capture shown on desktop (`block max-md:hidden`)
  - OR use a state-based approach with `useEffect` to detect touch capability and conditionally set the capture attribute
- The CSS approach (two inputs) is simpler and avoids hydration mismatches. Use it.
- Both inputs should share the same `onChange` handler and `accept="image/*"` attribute

**5. Form dialogs to full-screen sheets on mobile:**
For each form dialog component that uses shadcn Dialog, add responsive behavior so they become full-screen on mobile. The approach:
- Add responsive classes to DialogContent: `max-md:h-screen max-md:max-h-screen max-md:w-screen max-md:max-w-screen max-md:rounded-none max-md:border-0`
- This makes Dialog cover the full screen on mobile while remaining a centered modal on desktop
- Apply to all form dialogs:
  - `components/admin/categories/category-form-dialog.tsx`
  - `components/admin/companies/company-form-dialog.tsx`
  - `components/admin/divisions/division-form-dialog.tsx`
  - `components/admin/locations/location-form-dialog.tsx`
  - `components/admin/users/user-form-dialog.tsx`
- For `components/requests/request-triage-dialog.tsx` — same treatment if it uses Dialog
- For `components/profile/profile-sheet.tsx` — already uses Sheet, make it full-width on mobile if not already

**6. Table horizontal scroll on mobile (components/data-table/data-table.tsx):**
Ensure the DataTable wrapper has `overflow-x-auto` so tables scroll horizontally on narrow screens. The table should NOT stack to cards (per user decision: "Tables remain as horizontally scrollable tables on mobile"). Add `min-w-[600px]` or similar to the inner table element to prevent column crushing.

**7. Data table pagination and toolbar mobile adjustments:**
- `data-table-pagination.tsx`: Stack pagination controls vertically on mobile (`max-md:flex-col max-md:gap-2`)
- `data-table-toolbar.tsx`: Wrap filter controls on mobile (`max-md:flex-wrap`)

**8. Lightbox mobile adjustments (components/requests/request-photo-lightbox.tsx):**
Ensure the lightbox works on mobile — full-screen overlay should be touch-friendly with native pinch-to-zoom support (already noted in Phase 4 implementation).
  </action>
  <verify>
    <automated>cd /Users/melfice/code/ga && grep -l "MobileMenu" app/\(dashboard\)/layout.tsx components/mobile-menu.tsx && grep -l "capture" components/requests/request-photo-upload.tsx && npm run build 2>&1 | tail -5</automated>
    <manual>Resize browser to mobile width. Verify: sidebar becomes hamburger, dialogs go full-screen, tables scroll horizontally, photo upload offers camera on mobile.</manual>
  </verify>
  <done>Sidebar collapses to hamburger menu on mobile. Form dialogs become full-screen on mobile. Photo upload uses native camera capture on mobile. Tables scroll horizontally. All layout adjustments use desktop-first max-* breakpoints.</done>
</task>

<task type="auto">
  <name>Task 2: Breadcrumb navigation, error pages, and UI consistency pass</name>
  <files>
    app/(dashboard)/page.tsx
    app/(dashboard)/requests/page.tsx
    app/(dashboard)/requests/new/page.tsx
    app/(dashboard)/requests/[id]/page.tsx
    app/(dashboard)/admin/settings/page.tsx
    app/(dashboard)/admin/users/page.tsx
    app/not-found.tsx
    app/unauthorized/page.tsx
    app/(auth)/login/page.tsx
    app/(auth)/reset-password/page.tsx
    app/(auth)/update-password/page.tsx
  </files>
  <action>
**1. Breadcrumb navigation on all interior pages:**
Add full-path breadcrumbs using shadcn Breadcrumb components. Import from `@/components/ui/breadcrumb`:
`Breadcrumb, BreadcrumbList, BreadcrumbItem, BreadcrumbLink, BreadcrumbPage, BreadcrumbSeparator`

The request detail page (`requests/[id]/page.tsx`) already has breadcrumbs — use it as the reference pattern.

Add breadcrumbs to these pages:

- **Dashboard page** (`app/(dashboard)/page.tsx`): No breadcrumb needed — it's the root
- **Request list** (`app/(dashboard)/requests/page.tsx`): `Dashboard > Requests`
- **New request** (`app/(dashboard)/requests/new/page.tsx`): `Dashboard > Requests > New Request`
- **Request detail** (`app/(dashboard)/requests/[id]/page.tsx`): Already has `Dashboard > Requests > {display_id}` — verify it's correct
- **Settings** (`app/(dashboard)/admin/settings/page.tsx`): `Dashboard > Settings`
- **Users** (`app/(dashboard)/admin/users/page.tsx`): `Dashboard > Users`
- **Audit trail** (if created by Plan 02): `Dashboard > Settings > Audit Trail`

For any pages from Phases 5-8 that exist at execution time (jobs, inventory, maintenance, approvals), add breadcrumbs following the same pattern. Do NOT create files for routes that don't exist.

Breadcrumb placement: At the top of the page content, before the page header, with `mb-4` spacing.

**2. Error pages:**

**404 Page (app/not-found.tsx):**
Create a global not-found page:
- Centered layout with max-w-md
- Icon: `FileQuestion` or `SearchX` from lucide-react, size 48, muted color
- Heading: "Page not found" (text-2xl font-semibold)
- Description: "The page you're looking for doesn't exist or has been moved." (text-muted-foreground)
- CTA button: "Go to Dashboard" linking to "/" — use shadcn Button with `asChild` wrapping a Next.js Link
- No custom illustrations — icon + text only (per user decision)

**Unauthorized page (app/unauthorized/page.tsx):**
Update the existing unauthorized page for consistency:
- Same centered layout as 404
- Icon: `ShieldX` or `Lock` from lucide-react, size 48, muted color
- Heading: "Access Denied"
- Description: "You don't have permission to view this page."
- CTA button: "Go to Dashboard" linking to "/"
- Remove any `dark:` classes if they still exist (should be removed by Plan 03 Task 1)

**3. UI consistency pass across all pages:**
Review and fix inconsistencies across all existing pages:

- **Spacing**: Ensure consistent page padding (p-6 desktop, p-4 mobile from Task 1). Page headers should have consistent spacing (space-y-1 for title+description, mb-6 before content).
- **Typography**: Page titles should all be `text-2xl font-bold tracking-tight`. Page descriptions should be `text-muted-foreground`.
- **Empty states**: For any page that shows "no data" states (empty request list, no users), ensure they use: icon (from lucide-react, size 48, `text-muted-foreground`), short message text (`text-lg font-medium`), optional CTA button. No custom illustrations.
- **Button consistency**: Primary action buttons (New Request, Add User, etc.) should all use the same variant and size. Destructive actions should use `variant="destructive"`.
- **Auth pages** (login, reset-password, update-password): Ensure consistent card layout, spacing, and typography across all three. Remove any leftover dark: classes not caught by Plan 03.

**4. Additional responsive adjustments for all pages:**
- Dashboard: heading and stats cards should stack on mobile
- Settings: tabs should be scrollable horizontally on mobile if they overflow
- Users: company selector and add button should wrap on mobile
- Request detail: two-column layout already stacks below lg (verify with `max-lg:grid-cols-1`)
- Auth pages: ensure login card is properly sized on mobile (max-w-sm, padding adjustments)
  </action>
  <verify>
    <automated>cd /Users/melfice/code/ga && grep -l "Breadcrumb" app/\(dashboard\)/requests/page.tsx app/\(dashboard\)/admin/settings/page.tsx app/\(dashboard\)/admin/users/page.tsx && cat app/not-found.tsx | head -5 && npm run build 2>&1 | tail -5</automated>
    <manual>Check all pages for breadcrumbs. View 404 page at invalid URL. Check unauthorized page. Verify consistent typography and spacing across pages.</manual>
  </verify>
  <done>All interior pages have full-path breadcrumb navigation. 404 and unauthorized pages have consistent styling with icon + text + CTA. All pages have consistent spacing, typography, and button styling. Auth pages have consistent card layout.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of mobile responsiveness, breadcrumbs, and UI consistency</name>
  <action>
    Human verifies the mobile responsive layout, breadcrumb navigation, error pages, and UI consistency implemented in Tasks 1 and 2.
  </action>
  <what-built>Mobile responsive layout (hamburger sidebar, full-screen dialogs, camera capture, horizontal table scroll), breadcrumb navigation on all pages, standardized error pages, UI consistency across the application.</what-built>
  <how-to-verify>
    1. Open the app in a mobile viewport (use browser DevTools, resize to ~375px width)
    2. Verify sidebar is hidden and hamburger menu appears at the top
    3. Click hamburger — sidebar slides in as a Sheet overlay. Click a nav link — sheet closes
    4. Navigate to /requests — verify breadcrumb shows "Dashboard > Requests"
    5. Navigate to a request detail — verify breadcrumb shows "Dashboard > Requests > REQ-YYYY-NNNN"
    6. On mobile, try uploading a photo — should offer camera capture
    7. Open a form dialog (e.g., Add Company in Settings) — should be full-screen on mobile
    8. View request list table on mobile — should scroll horizontally
    9. Visit an invalid URL (e.g., /nonexistent) — verify 404 page with icon + text + "Go to Dashboard" button
    10. Check overall consistency: page titles, spacing, button styles across dashboard, requests, settings, users
  </how-to-verify>
  <verify>Human visual inspection of all 10 verification steps above</verify>
  <done>User confirms mobile layout, breadcrumbs, error pages, and UI consistency are all correct</done>
  <resume-signal>Type "approved" if everything looks correct, or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. Mobile hamburger menu works on narrow viewports
2. All form dialogs become full-screen on mobile
3. Photo upload offers camera capture on mobile
4. Tables scroll horizontally on mobile
5. Breadcrumbs present on all interior pages
6. 404 and unauthorized pages have consistent, branded styling
7. UI is consistent across all pages (spacing, typography, buttons)
8. `npm run build` passes
</verification>

<success_criteria>
- GA Staff can use the application on a mobile device to view requests and navigate
- Sidebar collapses to hamburger on mobile, opens as Sheet
- All form dialogs are full-screen on mobile for usability
- Photo upload leverages native camera on mobile
- Full-path breadcrumbs on every interior page
- Consistent error pages and empty states
- No visual regressions on desktop
</success_criteria>

<output>
After completion, create `.planning/phases/09-polish-integration/09-04-SUMMARY.md`
</output>
