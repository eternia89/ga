---
phase: 05-jobs-approvals
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - components/jobs/job-detail-info.tsx
  - components/jobs/job-detail-actions.tsx
  - components/jobs/job-detail-client.tsx
  - app/(dashboard)/jobs/[id]/page.tsx
autonomous: true
gap_closure: true
requirements:
  - REQ-JOB-008
  - REQ-APR-004

must_haves:
  truths:
    - "Estimated cost is editable inline in the job info panel"
    - "Job detail page merges view and edit into a single page"
    - "Users with edit permission can toggle fields to editable mode"
  artifacts:
    - path: "components/jobs/job-detail-info.tsx"
      provides: "Inline-editable fields with budget editing in info panel"
    - path: "components/jobs/job-detail-actions.tsx"
      provides: "Budget editing moved out of actions into info panel"
  key_links:
    - from: "components/jobs/job-detail-info.tsx"
      to: "app/actions/job-actions.ts"
      via: "updateJobBudget server action call"
      pattern: "updateJobBudget"
---

<objective>
Merge job view and edit into a single inline-editing experience on the detail page, and move estimated cost editing inline into the info panel.

Purpose: User feedback indicates the separate edit form and detached budget section create poor UX. The detail page should allow inline editing of fields for users with permission, matching the request detail pattern.
Output: Job detail page with inline-editable fields and budget editing in the info panel.
</objective>

<execution_context>
@/Users/melfice/.claude/get-shit-done/workflows/execute-plan.md
@/Users/melfice/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-jobs-approvals/05-UAT.md
@.planning/phases/05-jobs-approvals/05-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move budget editing inline and merge view/edit into single page</name>
  <files>
    app/(dashboard)/jobs/[id]/page.tsx
    components/jobs/job-detail-info.tsx
    components/jobs/job-detail-actions.tsx
    components/jobs/job-detail-client.tsx
  </files>
  <action>
    **Move budget editing inline (UAT test 4 — estimated cost):**
    In `components/jobs/job-detail-actions.tsx`, there is currently a separate budget input section (with `canEditBudget` guard, Input field, and "Update Budget" button). This entire budget editing section should be moved INTO `components/jobs/job-detail-info.tsx` where the estimated cost is already displayed.

    In `job-detail-info.tsx`:
    1. Add props: `currentUserId`, `currentUserRole`, `onActionSuccess` (if not already present)
    2. Where estimated cost is displayed (currently a read-only `formatIDR(job.estimated_cost)` display), make it conditionally editable:
       - If user has edit permission (isPIC or isGaLeadOrAdmin) AND status is `in_progress` AND not already budget-approved (`!job.approved_at`):
         - Show the cost value with a small edit icon (Pencil) next to it
         - On click, toggle to an inline Input field with Rp prefix and Save/Cancel buttons
         - Save calls `updateJobBudget` server action and calls `onActionSuccess`
       - Otherwise, display as read-only `formatIDR` value
    3. Remove the budget section from `job-detail-actions.tsx` (the `canEditBudget` condition, budget state, budget Input, and "Update Budget" button)

    **Merge view/edit into single page (UAT test 4 — inline editing):**
    The current job detail is read-only. Add an "Edit" button for users with edit permission (GA Lead/Admin for any active job). When clicked:
    1. Editable fields (title, description, location, category, priority) switch from display text to form inputs (same pattern as request detail's inline triage)
    2. Show Save/Cancel buttons when in edit mode
    3. Save calls `updateJob` server action (already exists in job-actions.ts)
    4. Cancel reverts to display mode

    Implementation approach:
    - Add `isEditing` state to `job-detail-info.tsx`
    - Add `canEdit` check: `isGaLeadOrAdmin && !['completed', 'cancelled'].includes(job.status)`
    - Add Edit button in the header area (near the status badges) when `canEdit && !isEditing`
    - When `isEditing`, render each editable field as its form control instead of plain text
    - Import `updateJob` from job-actions.ts and call on save
    - Pass `categories`, `locations`, `users` as props from the parent for Combobox/Select options (these are already available on the page.tsx server component — pass them through job-detail-client.tsx if not already passed)

    Update `app/(dashboard)/jobs/[id]/page.tsx` (server component) to fetch categories, locations, and users data and pass them as props to the client component. The server component may already fetch some of these — check and add any missing fetches.

    Update `job-detail-client.tsx` to accept and pass through the necessary props (categories, locations, users, currentUserId, currentUserRole, onActionSuccess) to `JobDetailInfo`.
  </action>
  <verify>
    <automated>cd /Users/melfice/code/ga && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Check that budget section is removed from job-detail-actions.tsx and budget edit is inline in job-detail-info.tsx</manual>
  </verify>
  <done>Estimated cost is editable inline in the info panel. Job detail page has an Edit button that toggles fields between display and editable mode. Budget section removed from actions panel. All editable fields use proper form controls with Combobox for large lists.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. `npm run build` succeeds
3. Budget editing is in the info panel, not in the actions section
4. Edit button visible for GA Lead/Admin on active jobs
5. Fields toggle between display and edit mode
6. Save calls updateJob action, Cancel reverts to display
</verification>

<success_criteria>
UAT gaps for test 4 (estimated cost inline + view/edit merge) fully resolved.
</success_criteria>

<output>
After completion, create `.planning/phases/05-jobs-approvals/05-07-SUMMARY.md`
</output>
