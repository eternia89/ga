---
phase: 05-jobs-approvals
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - app/actions/job-actions.ts
  - components/jobs/job-detail-info.tsx
  - components/jobs/job-detail-actions.tsx
  - components/jobs/job-comment-form.tsx
  - app/(dashboard)/jobs/[id]/page.tsx
autonomous: true
gap_closure: true
requirements:
  - REQ-JOB-004
  - REQ-JOB-005
  - REQ-JOB-006
  - REQ-JOB-008
  - REQ-APR-003

must_haves:
  truths:
    - "Job comments include company_id and insert successfully"
    - "Job detail page shows a single header (no duplication)"
    - "Comment form is hidden on completed and cancelled jobs"
    - "Approval buttons clearly label 'Approve Budget' / 'Reject Budget'"
    - "Reassign is available on assigned, in_progress, and pending_approval statuses"
    - "Approval page only shows approve/reject budget actions with no operational controls"
  artifacts:
    - path: "app/actions/job-actions.ts"
      provides: "addJobComment with company_id in insert payload"
    - path: "components/jobs/job-detail-actions.tsx"
      provides: "Expanded canReassign, renamed approval buttons, no cancel on approval page"
    - path: "components/jobs/job-detail-info.tsx"
      provides: "Job detail without duplicated header"
    - path: "components/jobs/job-comment-form.tsx"
      provides: "Comment form hidden on terminal statuses"
  key_links:
    - from: "app/actions/job-actions.ts"
      to: "job_comments table"
      via: "supabase insert with company_id"
      pattern: "company_id.*job\\.company_id"
---

<objective>
Fix 6 UAT gaps: 1 blocker (addJobComment missing company_id), 3 minor UX issues (duplicated header, comment form on terminal jobs, approval button labeling), 1 major permission fix (reassign availability), and 1 minor scope fix (approval page financial-only).

Purpose: Resolve the only blocker preventing comment creation plus 5 quick UX fixes identified during user acceptance testing.
Output: All 6 gaps closed with clean build.
</objective>

<execution_context>
@/Users/melfice/.claude/get-shit-done/workflows/execute-plan.md
@/Users/melfice/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-jobs-approvals/05-UAT.md
@.planning/phases/05-jobs-approvals/05-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix addJobComment blocker and job detail header duplication</name>
  <files>
    app/actions/job-actions.ts
    app/(dashboard)/jobs/[id]/page.tsx
    components/jobs/job-detail-info.tsx
  </files>
  <action>
    **Fix 1 — addJobComment missing company_id (BLOCKER, UAT test 5):**
    In `app/actions/job-actions.ts`, find the `addJobComment` action. The `.insert()` call is missing `company_id`. Add `company_id: job.company_id` to the insert payload object. The job is already fetched with `.select('id, assigned_to, company_id')` so the value is available.

    **Fix 2 — Duplicated header in job detail (UAT test 4):**
    The job detail page has the header (display_id, status badge, priority badge, title) rendered BOTH in:
    - `app/(dashboard)/jobs/[id]/page.tsx` (the server component)
    - `components/jobs/job-detail-info.tsx` (the client component)

    Check which one renders the header. Remove the duplicate. Keep the header in `job-detail-info.tsx` (consistent with request detail pattern where info component owns the header) and remove any duplicate rendering from `page.tsx`. If page.tsx wraps with a heading or repeats the job ID/title above the client component, remove that redundant section.
  </action>
  <verify>
    <automated>cd /Users/melfice/code/ga && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Grep for company_id in addJobComment insert to confirm fix</manual>
  </verify>
  <done>addJobComment includes company_id in insert payload. Job detail page shows exactly one header section with display_id, status, priority, and title.</done>
</task>

<task type="auto">
  <name>Task 2: Fix reassign scope, approval button labels, comment visibility, and approval page scope</name>
  <files>
    components/jobs/job-detail-actions.tsx
    components/jobs/job-comment-form.tsx
  </files>
  <action>
    **Fix 3 — Reassign available beyond 'assigned' status (UAT test 6):**
    In `components/jobs/job-detail-actions.tsx`, find the `canReassign` condition. Currently it checks:
    ```
    const canReassign = isGaLeadOrAdmin && job.status === 'assigned';
    ```
    Change to allow reassignment on any active status before completion:
    ```
    const canReassign = isGaLeadOrAdmin && ['assigned', 'in_progress', 'pending_approval'].includes(job.status);
    ```

    **Fix 4 — Approval button labels (UAT test 7):**
    In the same file, find the Approve and Reject buttons shown when `canApproveReject` is true. Change:
    - "Approve" -> "Approve Budget"
    - "Reject" -> "Reject Budget"
    Also update the ThumbsUp/ThumbsDown icon labels in the button text. Search for `ThumbsUp` and `ThumbsDown` usage near the approve/reject section.

    **Fix 5 — Approval page financial scope (UAT test 9):**
    In the same file, the cancel action button is currently shown via `canCancel` which is:
    ```
    const canCancel = isGaLeadOrAdmin && !['completed', 'cancelled'].includes(job.status);
    ```
    This shows the cancel button even for finance_approver viewing from the approval queue. The fix: when the user is `finance_approver` (not admin), do NOT show operational actions like Cancel. Adjust `canCancel` to exclude `finance_approver` role:
    ```
    const canCancel = isGaLeadOrAdmin && !isFinanceApproverOnly && !['completed', 'cancelled'].includes(job.status);
    ```
    Add a helper: `const isFinanceApproverOnly = currentUserRole === 'finance_approver';`
    This means admin (who has both roles) still sees cancel, but pure finance_approver does not.

    **Fix 6 — Comments disabled on completed/cancelled jobs (UAT test 11):**
    In `components/jobs/job-comment-form.tsx`, wrap the component render with a status check. The component receives `job` or `jobId` props. If it doesn't receive the job status, add a `jobStatus` prop. If the status is 'completed' or 'cancelled', return `null` (or a disabled message like "Comments are closed for this job").

    Check how `JobCommentForm` is used in `job-detail-client.tsx` — it likely receives a `jobId` prop. If it doesn't receive status, pass `jobStatus={job.status}` from the parent. Then in the comment form component, check:
    ```tsx
    if (['completed', 'cancelled'].includes(jobStatus)) {
      return null;
    }
    ```
  </action>
  <verify>
    <automated>cd /Users/melfice/code/ga && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Verify canReassign includes in_progress and pending_approval</manual>
  </verify>
  <done>Reassign works on assigned/in_progress/pending_approval jobs. Buttons say "Approve Budget" / "Reject Budget". Cancel button hidden for finance_approver. Comment form hidden on completed/cancelled jobs.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. `npm run build` succeeds
3. addJobComment insert includes company_id field
4. Job detail shows single header (not duplicated)
5. canReassign includes assigned, in_progress, pending_approval
6. Approve/Reject buttons labeled as "Approve Budget" / "Reject Budget"
7. Comment form returns null on completed/cancelled
8. finance_approver does not see Cancel button
</verification>

<success_criteria>
All 6 UAT gaps (tests 4-header, 5, 6, 7, 9, 11) resolved with clean TypeScript compilation and build.
</success_criteria>

<output>
After completion, create `.planning/phases/05-jobs-approvals/05-06-SUMMARY.md`
</output>
