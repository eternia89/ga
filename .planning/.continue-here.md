---
phase: quick-tasks
task: in-progress
status: partial
last_updated: 2026-03-09T15:00:00.000Z
---

<current_state>
Working on 3 quick tasks from user's batch. 2 of 3 complete, 1 remaining (partially started, reverted).
</current_state>

<completed_work>

**This session:**

- Quick 31 (task 34): Fix DialogTitle accessibility warning in all 5 view modals — moved DialogTitle outside conditional blocks. Commit: af12391
- Quick 32 (task 35): Only request creator can accept/reject completed work — removed admin override from acceptRequest, rejectCompletedWork, and both UI components. Commit: 2bacdd3

**Also this session:**
- Changed GSD settings: model_profile from quality → balanced, auto_advance enabled
- Added model_overrides: planner=sonnet, researcher=sonnet (user edited config.json directly)

</completed_work>

<remaining_work>

**Task 3 (NOT STARTED — reverted partial work):**
Move Save Changes button to sticky bottom bar LEFT side on both job detail and asset detail pages.

Key context gathered:
- **Job detail** (`components/jobs/job-detail-client.tsx`): Two-column grid layout. Save button is currently in `job-detail-info.tsx` (line ~400) using `handleEditSave` function. Need to lift save trigger to parent and render in a sticky bottom bar.
- **Asset detail** (`components/assets/asset-detail-client.tsx`): Two-column grid layout. The edit form uses `id="asset-edit-form"` pattern. Save button is only in the modal (`asset-view-modal.tsx` line 510). The detail page has NO save button currently — `AssetEditForm` is rendered inline by `AssetDetailInfo` when `canEdit=true` but there's no submit button on the page itself.
- Both pages need: a sticky bottom bar below the grid, with Save Changes button on the LEFT side (not right).
- Pattern: `<div className="sticky bottom-0 ...">` after the grid div, with `justify-start` for left alignment.
- Job: needs a `saveRef` or `onSubmittingChange` callback to lift save state from `job-detail-info.tsx` to `job-detail-client.tsx`
- Asset: can use `type="submit" form="asset-edit-form"` since the form already has an id

**User also mentioned (incomplete sentence):**
"request page: after" — user was asked about this but never clarified. Ask again next session.

</remaining_work>

<decisions_made>
- GSD model_profile: balanced with planner+researcher overridden to sonnet
- auto_advance: enabled
- DialogTitle fix: static text ("Request Details", "Job Details" etc.) instead of dynamic display_id
- Request acceptance: requester-only (no admin override)
- Save button: left side of sticky bottom bar (not right side)
</decisions_made>

<blockers>
- None
</blockers>

<next_action>
1. Complete Task 3: Move save changes to sticky bottom bar (left side) on job detail and asset detail pages
2. Ask user about incomplete "request page: after" requirement
</next_action>
