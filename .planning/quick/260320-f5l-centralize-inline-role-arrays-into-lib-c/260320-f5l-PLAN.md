---
phase: quick
plan: 260320-f5l
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/constants/roles.ts
  - app/(dashboard)/page.tsx
  - app/(dashboard)/requests/page.tsx
  - app/(dashboard)/jobs/page.tsx
  - app/(dashboard)/approvals/page.tsx
  - app/api/exports/requests/route.ts
  - app/api/exports/jobs/route.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "No inline hardcoded ['ga_lead', 'admin', 'finance_approver'] arrays remain anywhere in the codebase"
    - "No inline hardcoded ['ga_lead', 'admin', 'ga_staff'] array remains in jobs/page.tsx"
    - "OPERATIONAL_ROLES constant is exported from lib/constants/roles.ts"
    - "All 7 files compile without TypeScript errors"
    - "Build succeeds with no regressions"
  artifacts:
    - path: "lib/constants/roles.ts"
      provides: "OPERATIONAL_ROLES constant"
      exports: ["ROLES", "Role", "GA_ROLES", "LEAD_ROLES", "OPERATIONAL_ROLES"]
    - path: "app/(dashboard)/page.tsx"
      provides: "Dashboard page using imported OPERATIONAL_ROLES"
      contains: "OPERATIONAL_ROLES"
    - path: "app/api/exports/requests/route.ts"
      provides: "Request export route using imported OPERATIONAL_ROLES"
      contains: "OPERATIONAL_ROLES"
    - path: "app/api/exports/jobs/route.ts"
      provides: "Job export route using imported OPERATIONAL_ROLES"
      contains: "OPERATIONAL_ROLES"
  key_links:
    - from: "app/(dashboard)/page.tsx"
      to: "lib/constants/roles.ts"
      via: "import { OPERATIONAL_ROLES }"
      pattern: "import.*OPERATIONAL_ROLES.*from.*@/lib/constants/roles"
    - from: "app/(dashboard)/requests/page.tsx"
      to: "lib/constants/roles.ts"
      via: "import { OPERATIONAL_ROLES }"
      pattern: "import.*OPERATIONAL_ROLES.*from.*@/lib/constants/roles"
    - from: "app/(dashboard)/jobs/page.tsx"
      to: "lib/constants/roles.ts"
      via: "import { OPERATIONAL_ROLES, GA_ROLES }"
      pattern: "import.*OPERATIONAL_ROLES.*from.*@/lib/constants/roles"
    - from: "app/(dashboard)/approvals/page.tsx"
      to: "lib/constants/roles.ts"
      via: "import { OPERATIONAL_ROLES }"
      pattern: "import.*OPERATIONAL_ROLES.*from.*@/lib/constants/roles"
    - from: "app/api/exports/requests/route.ts"
      to: "lib/constants/roles.ts"
      via: "import { OPERATIONAL_ROLES }"
      pattern: "import.*OPERATIONAL_ROLES.*from.*@/lib/constants/roles"
    - from: "app/api/exports/jobs/route.ts"
      to: "lib/constants/roles.ts"
      via: "import { OPERATIONAL_ROLES }"
      pattern: "import.*OPERATIONAL_ROLES.*from.*@/lib/constants/roles"
---

<objective>
Centralize all inline hardcoded role arrays into lib/constants/roles.ts by adding an OPERATIONAL_ROLES constant and replacing 7 inline role arrays across 7 files with proper imports.

Purpose: Eliminate duplicated role set definitions that are fragile to maintain -- if OPERATIONAL_ROLES membership changes, only one file needs updating instead of 7.
Output: Updated lib/constants/roles.ts with OPERATIONAL_ROLES export; 6 files using OPERATIONAL_ROLES import; 1 file also replacing inline GA_ROLES duplicate.
</objective>

<execution_context>
@/Users/melfice/code/ga/.claude/get-shit-done/workflows/execute-plan.md
@/Users/melfice/code/ga/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@lib/constants/roles.ts
@.planning/quick/260320-f5l-centralize-inline-role-arrays-into-lib-c/260320-f5l-RESEARCH.md

<interfaces>
<!-- Current exports from lib/constants/roles.ts -->
From lib/constants/roles.ts:
```typescript
export const ROLES = {
  GENERAL_USER: 'general_user',
  GA_STAFF: 'ga_staff',
  GA_LEAD: 'ga_lead',
  FINANCE_APPROVER: 'finance_approver',
  ADMIN: 'admin',
} as const;

export type Role = (typeof ROLES)[keyof typeof ROLES];

/** GA Staff, GA Lead, Admin -- operational roles that manage assets/jobs */
export const GA_ROLES = [ROLES.GA_STAFF, ROLES.GA_LEAD, ROLES.ADMIN] as const;

/** GA Lead, Admin -- leadership roles with elevated permissions */
export const LEAD_ROLES = [ROLES.GA_LEAD, ROLES.ADMIN] as const;
```

<!-- Established pattern from app/api/exports/inventory/route.ts -->
From app/api/exports/inventory/route.ts:
```typescript
import { GA_ROLES } from '@/lib/constants/roles';
const EXPORT_ROLES: readonly string[] = GA_ROLES;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add OPERATIONAL_ROLES constant to lib/constants/roles.ts</name>
  <files>lib/constants/roles.ts</files>
  <action>
Add the OPERATIONAL_ROLES constant after the existing LEAD_ROLES definition in lib/constants/roles.ts:

```typescript
/** GA Lead, Admin, Finance Approver -- roles with full operational visibility (dashboard, exports, approvals) */
export const OPERATIONAL_ROLES = [ROLES.GA_LEAD, ROLES.ADMIN, ROLES.FINANCE_APPROVER] as const;
```

This follows the exact same pattern as GA_ROLES and LEAD_ROLES (uses ROLES enum values, `as const`, JSDoc comment).
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>lib/constants/roles.ts exports OPERATIONAL_ROLES alongside existing GA_ROLES and LEAD_ROLES.</done>
</task>

<task type="auto">
  <name>Task 2: Replace all 7 inline role arrays with constant imports</name>
  <files>
    app/(dashboard)/page.tsx
    app/(dashboard)/requests/page.tsx
    app/(dashboard)/jobs/page.tsx
    app/(dashboard)/approvals/page.tsx
    app/api/exports/requests/route.ts
    app/api/exports/jobs/route.ts
  </files>
  <action>
Replace inline role arrays in 6 files with imports from @/lib/constants/roles. None of these files currently import from roles.ts, so each needs a new import line added.

**1. app/(dashboard)/page.tsx (line 35):**
- Add import: `import { OPERATIONAL_ROLES } from '@/lib/constants/roles';`
- Remove the local const: `const OPERATIONAL_ROLES = ['ga_lead', 'admin', 'finance_approver'];`
- The rest of the file already uses `OPERATIONAL_ROLES` by name, so no other changes needed.
- For any `.includes(profile.role)` call, use the cast pattern: `(OPERATIONAL_ROLES as readonly string[]).includes(profile.role)`

**2. app/(dashboard)/requests/page.tsx (line 171):**
- Add import: `import { OPERATIONAL_ROLES } from '@/lib/constants/roles';`
- Replace `['ga_lead', 'admin', 'finance_approver'].includes(profile.role)` with `(OPERATIONAL_ROLES as readonly string[]).includes(profile.role)`

**3. app/(dashboard)/jobs/page.tsx (lines 225 AND 228):**
- Add import: `import { OPERATIONAL_ROLES, GA_ROLES } from '@/lib/constants/roles';`
- Line 225: Replace `['ga_lead', 'admin', 'finance_approver'].includes(profile.role)` with `(OPERATIONAL_ROLES as readonly string[]).includes(profile.role)`
- Line 228: Replace `['ga_lead', 'admin', 'ga_staff'].includes(profile.role)` with `(GA_ROLES as readonly string[]).includes(profile.role)`

**4. app/(dashboard)/approvals/page.tsx (lines 34-35):**
- Add import: `import { OPERATIONAL_ROLES } from '@/lib/constants/roles';`
- Replace `!['finance_approver', 'ga_lead', 'admin'].includes(profile.role)` with `!(OPERATIONAL_ROLES as readonly string[]).includes(profile.role)`

**5. app/api/exports/requests/route.ts (line 11):**
- Add import: `import { OPERATIONAL_ROLES } from '@/lib/constants/roles';`
- Replace `const EXPORT_ROLES = ['ga_lead', 'admin', 'finance_approver'];` with `const EXPORT_ROLES: readonly string[] = OPERATIONAL_ROLES;`
- This matches the exact pattern from app/api/exports/inventory/route.ts.

**6. app/api/exports/jobs/route.ts (line 11):**
- Add import: `import { OPERATIONAL_ROLES } from '@/lib/constants/roles';`
- Replace `const EXPORT_ROLES = ['ga_lead', 'admin', 'finance_approver'];` with `const EXPORT_ROLES: readonly string[] = OPERATIONAL_ROLES;`
- Same pattern as exports/requests/route.ts above.

IMPORTANT: Use the `(CONSTANT as readonly string[]).includes(...)` cast pattern for all .includes() calls. This is required because `as const` arrays have narrow literal types that reject `string` in .includes(). This is the established convention in the codebase.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
All 7 inline role arrays eliminated. Specifically:
- 0 occurrences of inline ['ga_lead', 'admin', 'finance_approver'] remain (was 6)
- 0 occurrences of inline ['ga_lead', 'admin', 'ga_staff'] remain in jobs/page.tsx (was 1)
- 6 files now import OPERATIONAL_ROLES from @/lib/constants/roles
- 1 file (jobs/page.tsx) also imports GA_ROLES from @/lib/constants/roles
- Build succeeds with no TypeScript errors
  </done>
</task>

</tasks>

<verification>
After both tasks complete, verify no inline role arrays remain:

1. `grep -rn "'\''ga_lead'\''.*'\''admin'\''.*'\''finance_approver'\''" app/ lib/ --include="*.ts" --include="*.tsx"` returns no results
2. `grep -rn "'\''finance_approver'\''.*'\''ga_lead'\''.*'\''admin'\''" app/ lib/ --include="*.ts" --include="*.tsx"` returns no results
3. `grep -rn "'\''ga_lead'\''.*'\''admin'\''.*'\''ga_staff'\''" app/ lib/ --include="*.ts" --include="*.tsx"` returns no results
4. `npm run build` succeeds
</verification>

<success_criteria>
- OPERATIONAL_ROLES exported from lib/constants/roles.ts
- All 6 inline ['ga_lead', 'admin', 'finance_approver'] arrays replaced with OPERATIONAL_ROLES import
- Inline ['ga_lead', 'admin', 'ga_staff'] in jobs/page.tsx replaced with GA_ROLES import
- Production build passes with zero errors
- No remaining inline role arrays for sets that have constants
</success_criteria>

<output>
After completion, create `.planning/quick/260320-f5l-centralize-inline-role-arrays-into-lib-c/260320-f5l-SUMMARY.md`
</output>
