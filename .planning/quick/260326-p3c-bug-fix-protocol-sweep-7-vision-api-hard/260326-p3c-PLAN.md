# Sweep 7: Vision API Hardening

## Goal
Move Vision API key from URL query parameter to request header for defense-in-depth security.

## Findings (2 files)

| # | File | Issue | Fix |
|---|------|-------|-----|
| 1 | `app/api/vision/describe/route.ts:67` | API key in URL: `?key=${apiKey}` — logged by proxies | Moved to `x-goog-api-key` header |
| 2 | `app/api/uploads/entity-photos/route.ts:223` | Same pattern in fire-and-forget Vision call | Moved to `x-goog-api-key` header |

## Already Resolved (verified this sweep)
- Attachment ownership check — already present via `assertCompanyAccess` (quick-128)
- Entity-photos table name — already uses `inventory_items` (not `assets`)

## Pattern
Google Cloud APIs support `x-goog-api-key` header as alternative to `?key=` query param. URLs are logged by intermediate proxies/load balancers; headers are not.

## Status: COMPLETE
