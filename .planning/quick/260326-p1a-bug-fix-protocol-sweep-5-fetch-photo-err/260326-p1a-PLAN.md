# Sweep 5: Fetch Response + Photo Upload Error Handling

## Goal
Add user-visible error feedback to all fetch() calls that previously only logged to console or had zero error handling.

## Findings (4 issues across 4 files)

| # | File | Issue | Fix |
|---|------|-------|-----|
| 1 | `components/jobs/job-modal.tsx:1083` | `await fetch(...)` with zero error handling | Add response.ok check + setFeedback warning |
| 2 | `components/jobs/job-form.tsx:309-312` | Only console.warn, no user feedback | Show error message, don't navigate |
| 3 | `components/requests/request-submit-form.tsx:98-102` | Only console.warn, no user feedback | Show error message, don't navigate |
| 4 | `components/export-button.tsx:18-20` | Only console.error, invisible to user | Add error state + inline display |

## Pattern
Two-step mutations (create entity -> upload photos) where step 2 failure was invisible to users. Fix follows asset-submit-form.tsx pattern: show warning, early return, don't navigate away.

## Status: COMPLETE
