# Sweep 6: UI Convention Violations

## Goal
Fix remaining UI convention violations flagged in improvements.md.

## Findings

### Fixed
| # | Issue | Fix |
|---|-------|-----|
| 1 | `/inventory/new` page exists alongside AssetCreateDialog modal — violates "no separate `/new` pages" rule | Replaced with redirect to `/inventory?action=create` |

### Already Resolved (verified this sweep)
- `pb-20` on asset detail page — already present (quick-132)
- `getSession()` → `getUser()` — already fixed (quick-129), zero getSession() calls remain
- CSV export date format — already uses `dd-MM-yyyy`
- `roleColors`/`roleDisplay` — already extracted to `lib/constants/role-display.ts`
- `TYPE_COLORS` — already extracted to `lib/constants/checklist-types.ts`
- `JOB_STATUS_LABELS` — already extracted to `lib/constants/job-status.ts`
- `hover:text-blue-800/500` — zero non-standard hover colors remain
- `as any` casts — zero remain in components
- Entity-photos wrong table name — already uses `inventory_items`
- `DisplayId` adoption — 60+ usages, only 3 inline `font-mono` remain (2 for confirmation text, 1 for timestamp — all correct)
- `CreatedAtCell` adopted in all 5 table column files

## Status: COMPLETE
