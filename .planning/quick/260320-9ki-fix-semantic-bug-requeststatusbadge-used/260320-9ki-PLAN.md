---
phase: quick
plan: 260320-9ki
type: execute
wave: 1
depends_on: []
files_modified:
  - components/requests/request-detail-info.tsx
autonomous: true
requirements: [QUICK-260320-9ki]

must_haves:
  truths:
    - "Linked jobs in request detail show correct job status colors and labels (e.g., green for Completed, not gray fallback)"
    - "Request status badges elsewhere continue to render correctly"
  artifacts:
    - path: "components/requests/request-detail-info.tsx"
      provides: "Correct JobStatusBadge usage for linked jobs"
      contains: "JobStatusBadge"
  key_links:
    - from: "components/requests/request-detail-info.tsx"
      to: "components/jobs/job-status-badge.tsx"
      via: "import JobStatusBadge"
      pattern: "import.*JobStatusBadge.*from.*job-status-badge"
---

<objective>
Fix semantic bug where RequestStatusBadge is used to render job statuses in the linked jobs section of request-detail-info.tsx.

Purpose: Job statuses (created, assigned, completed, pending_approval, pending_completion_approval) are not in the request status map, causing them to fall through to raw DB values with gray fallback colors instead of proper labels and colors.
Output: Correct JobStatusBadge rendering for linked jobs in request detail view.
</objective>

<execution_context>
@/Users/melfice/code/ga/.claude/get-shit-done/workflows/execute-plan.md
@/Users/melfice/code/ga/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260320-9ki-fix-semantic-bug-requeststatusbadge-used/260320-9ki-RESEARCH.md

<interfaces>
<!-- JobStatusBadge component — the replacement -->
From components/jobs/job-status-badge.tsx:
```typescript
export function JobStatusBadge({ status }: { status: string }): JSX.Element;
// Maps job statuses (created, assigned, in_progress, pending_approval, pending_completion_approval, completed, cancelled) to correct colors/labels
```

<!-- RequestStatusBadge component — currently used incorrectly -->
From components/requests/request-status-badge.tsx:
```typescript
export function RequestStatusBadge({ status }: { status: string }): JSX.Element;
// Maps request statuses (submitted, triaged, in_progress, pending_acceptance, accepted, closed, rejected, cancelled) to colors/labels
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace RequestStatusBadge with JobStatusBadge for linked jobs</name>
  <files>components/requests/request-detail-info.tsx</files>
  <action>
In `components/requests/request-detail-info.tsx`:

1. **Remove line 15:** Delete `import { RequestStatusBadge } from './request-status-badge';` — this import is ONLY used on line 368 for job status rendering and nowhere else in this file.

2. **Add new import:** Add `import { JobStatusBadge } from '@/components/jobs/job-status-badge';` in the imports section (after the other component imports around line 14).

3. **Line 368:** Replace `<RequestStatusBadge status={job.status} />` with `<JobStatusBadge status={job.status} />`.

This is a 2-line change (swap import, swap component usage). No other modifications needed.
  </action>
  <verify>
    <automated>cd /Users/melfice/code/ga && npx grep-ast "JobStatusBadge" components/requests/request-detail-info.tsx && ! grep -q "RequestStatusBadge" components/requests/request-detail-info.tsx && echo "PASS: JobStatusBadge used, RequestStatusBadge removed" || echo "FAIL"</automated>
    <automated>cd /Users/melfice/code/ga && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Line 368 uses JobStatusBadge instead of RequestStatusBadge. The RequestStatusBadge import is removed from request-detail-info.tsx. Build passes with no errors.</done>
</task>

</tasks>

<verification>
- `grep -n "JobStatusBadge" components/requests/request-detail-info.tsx` shows import and usage
- `grep -n "RequestStatusBadge" components/requests/request-detail-info.tsx` returns nothing
- `npm run build` succeeds
</verification>

<success_criteria>
- Linked jobs in request detail view display correct job status badges with proper colors and labels
- No reference to RequestStatusBadge remains in request-detail-info.tsx
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/quick/260320-9ki-fix-semantic-bug-requeststatusbadge-used/260320-9ki-SUMMARY.md`
</output>
