---
phase: quick-260326-f3u
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/actions/profile-actions.ts
  - app/actions/pm-job-actions.ts
  - lib/validations/job-schema.ts
  - lib/validations/template-schema.ts
  - components/profile/password-change-dialog.tsx
  - app/(auth)/update-password/page.tsx
autonomous: true
requirements: [QUICK-260326-F3U]

must_haves:
  truths:
    - "All password string fields reject input longer than 255 characters at the schema level"
    - "itemId fields in PM job actions only accept valid UUIDs"
    - "photoUrls array rejects more than 20 entries"
    - "linked_request_ids array rejects more than 50 entries"
    - "checklist array rejects more than 100 items"
    - "Password HTML inputs enforce maxLength=255 in the browser"
  artifacts:
    - path: "app/actions/profile-actions.ts"
      provides: "Password schema with .max(255) on both fields"
      contains: ".max(255)"
    - path: "app/actions/pm-job-actions.ts"
      provides: "UUID validation on itemId, .max(20) on photoUrls"
      contains: ".uuid()"
    - path: "lib/validations/job-schema.ts"
      provides: ".max(50) on both linked_request_ids arrays"
      contains: ".max(50)"
    - path: "lib/validations/template-schema.ts"
      provides: ".max(100) on checklist array"
      contains: ".max(100"
    - path: "components/profile/password-change-dialog.tsx"
      provides: "Local schema .max(255) + Input maxLength={255}"
      contains: "maxLength={255}"
    - path: "app/(auth)/update-password/page.tsx"
      provides: "Raw input maxLength={255}"
      contains: "maxLength={255}"
  key_links:
    - from: "components/profile/password-change-dialog.tsx"
      to: "app/actions/profile-actions.ts"
      via: "Local schema mirrors action schema constraints"
      pattern: "\\.max\\(255\\)"
    - from: "app/actions/pm-job-actions.ts"
      to: "lib/validations/template-schema.ts"
      via: "itemId references ChecklistItem.id which is z.string().uuid()"
      pattern: "\\.uuid\\(\\)"
---

<objective>
Harden 5 Zod schema validation gaps by adding missing `.max()` constraints to unbounded strings/arrays and `.uuid()` to a bare string field. Also add `maxLength` HTML attributes to corresponding password inputs for browser-level enforcement.

Purpose: Prevent oversized payloads and malformed IDs from reaching server actions. All changes are additive constraints -- no breaking changes to existing data.
Output: 7 files updated with tightened validation constraints.
</objective>

<execution_context>
@/Users/melfice/code/ga/.claude/get-shit-done/workflows/execute-plan.md
@/Users/melfice/code/ga/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260326-f3u-schema-validation-hardening-add-max-to-p/260326-f3u-RESEARCH.md
@app/actions/profile-actions.ts
@app/actions/pm-job-actions.ts
@lib/validations/job-schema.ts
@lib/validations/template-schema.ts
@components/profile/password-change-dialog.tsx
@app/(auth)/update-password/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden action schemas and validation schemas</name>
  <files>app/actions/profile-actions.ts, app/actions/pm-job-actions.ts, lib/validations/job-schema.ts, lib/validations/template-schema.ts</files>
  <action>
Apply these exact Zod constraint additions (all changes are appending methods to existing chains):

**profile-actions.ts** (lines 38-39, inside changePassword .schema()):
- Line 38: `currentPassword: z.string().min(1, "Current password is required")` --> add `.max(255)` after `.min(1, ...)`
- Line 39: `newPassword: z.string().min(8, "Password must be at least 8 characters")` --> add `.max(255)` after `.min(8, ...)`

**pm-job-actions.ts** -- savePMChecklistItem schema (line 19):
- `itemId: z.string()` --> `itemId: z.string().uuid()`

**pm-job-actions.ts** -- savePMChecklistPhoto schema (lines 107-108):
- Line 107: `itemId: z.string()` --> `itemId: z.string().uuid()`
- Line 108: `photoUrls: z.array(z.string().max(2048))` --> `z.array(z.string().max(2048)).max(20)`

**job-schema.ts** -- createJobSchema (line 17):
- `linked_request_ids: z.array(z.string().uuid()).default([])` --> `z.array(z.string().uuid()).max(50).default([])`

**job-schema.ts** -- updateJobSchema (line 41):
- `linked_request_ids: z.array(z.string().uuid()).optional()` --> `z.array(z.string().uuid()).max(50).optional()`

**template-schema.ts** -- templateCreateSchema (line 52):
- `checklist: z.array(checklistItemSchema).min(1, 'At least one checklist item required')` --> append `.max(100, 'Maximum 100 checklist items')`
  </action>
  <verify>
    <automated>cd /Users/melfice/code/ga && grep -n "\.max(255)" app/actions/profile-actions.ts && grep -n "\.uuid()" app/actions/pm-job-actions.ts && grep -n "\.max(20)" app/actions/pm-job-actions.ts && grep -n "\.max(50)" lib/validations/job-schema.ts && grep -n "\.max(100" lib/validations/template-schema.ts && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>All 4 schema files have tightened constraints: password fields .max(255), itemId .uuid(), photoUrls .max(20), linked_request_ids .max(50), checklist .max(100). Build passes with no type errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add maxLength HTML attributes to password inputs</name>
  <files>components/profile/password-change-dialog.tsx, app/(auth)/update-password/page.tsx</files>
  <action>
**password-change-dialog.tsx** -- Two changes needed:

1. Update the LOCAL Zod schema (lines 33-36) to match the action schema constraints:
   - Line 34: `currentPassword: z.string().min(1, "Current password is required")` --> add `.max(255)`
   - Line 35: `newPassword: z.string().min(8, "Password must be at least 8 characters")` --> add `.max(255)`
   - Line 36: `confirmPassword: z.string().min(1, "Please confirm your password")` --> add `.max(255)`

2. Add `maxLength={255}` to ALL THREE `<Input type="password">` elements:
   - Line 110: `<Input type="password" {...field} />` --> `<Input type="password" maxLength={255} {...field} />`
   - Line 124: same change
   - Line 138: same change

**update-password/page.tsx** -- Add `maxLength={255}` to BOTH raw `<input>` elements:
   - Line 140: the new-password input --> add `maxLength={255}` attribute
   - Line 159: the confirm-password input --> add `maxLength={255}` attribute
  </action>
  <verify>
    <automated>cd /Users/melfice/code/ga && grep -n "maxLength" components/profile/password-change-dialog.tsx && grep -n "maxLength" app/\(auth\)/update-password/page.tsx && grep -c "\.max(255)" components/profile/password-change-dialog.tsx && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>password-change-dialog.tsx has .max(255) on all 3 local schema fields and maxLength={255} on all 3 Input elements. update-password/page.tsx has maxLength={255} on both raw input elements. Build passes.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. All 7 files contain the expected constraint additions (grep verification in each task)
3. No existing functionality broken -- all changes are additive constraints within safe limits
</verification>

<success_criteria>
- All 5 schema gaps closed: password .max(255), itemId .uuid(), photoUrls .max(20), linked_request_ids .max(50), checklist .max(100)
- Password UI inputs have maxLength={255} in both password-change-dialog and update-password page
- Local password schema in dialog matches action schema constraints
- Production build succeeds
</success_criteria>

<output>
After completion, create `.planning/quick/260326-f3u-schema-validation-hardening-add-max-to-p/260326-f3u-SUMMARY.md`
</output>
