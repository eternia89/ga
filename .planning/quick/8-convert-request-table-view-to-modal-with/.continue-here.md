---
phase: quick-8
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-03-05T05:35:35.835Z
---

<current_state>
Quick task 8: Convert request table "View" action to open a modal dialog instead of navigating to a detail page.

Tasks 1-2 are committed and built successfully. Task 3 (human verification) is in progress — user is testing and providing revision feedback. Three rounds of revisions have been committed. The executor's SUMMARY.md has NOT been created yet, and STATE.md has NOT been updated yet (those are post-verification steps in the quick workflow).
</current_state>

<completed_work>

- Task 1: Create RequestViewModal component (commit 24d9be7) - Done
- Task 2: Wire modal into request table and list page (commit 2cd22f5) - Done
- Revision 1 (commit a9b497a): Remove tabs → single layout with details left + timeline right. Convert PhotoLightbox to Dialog (proper ESC handling). Fix nav button overlap. Use close icon top-right. Linked jobs open in new tab. Add prev/next request navigation.
- Revision 2 (commit 47c31d1): Unify Save Changes + Triage into single "Update Request" button in bottom action bar. Only visible when form is dirty. Remove Triage button. Forms expose formId/onDirtyChange/onSubmittingChange for external submit.
</completed_work>

<remaining_work>

- Task 3: Human verification - user needs to test the latest changes on localhost:3000
- After verification passes: create 8-SUMMARY.md, update STATE.md, final commit (quick workflow steps 6.5-8)
- Potential additional revisions based on user testing feedback
</remaining_work>

<decisions_made>

- Single entity first (Requests) as proof-of-concept, will apply to Jobs/Assets/Templates/Schedules later
- Keep existing /requests/[id] detail page as fallback for direct URL access
- Modal size: max-w-6xl (~1152px)
- No tabs: split layout with details left, timeline right (grid-cols-[1fr_350px])
- Photo lightbox converted to Dialog component for proper ESC stacking (only frontmost modal closes)
- Linked jobs open in new tab (no modal-in-modal)
- Prev/next navigation arrows in modal header to switch between requests
- Single "Update Request" button in bottom bar replaces both "Save Changes" and "Triage" buttons
- Button only appears when form has dirty state (tracked via onDirtyChange callback)
- Forms use HTML form={formId} attribute pattern for external submit from action bar
- Triage server action auto-progresses submitted→triaged (no separate triage step needed)
</decisions_made>

<blockers>
- None currently
</blockers>

<context>
This is a --full mode quick task. After user verification passes, need to:
1. Run gsd-verifier (step 6.5 in quick workflow)
2. Create 8-SUMMARY.md
3. Update STATE.md with quick task row (with Status column for --full mode)
4. Final commit of docs artifacts

Key files modified:
- components/requests/request-view-modal.tsx (new, ~680 lines)
- components/requests/request-photo-lightbox.tsx (rewritten to use Dialog)
- components/requests/request-detail-info.tsx (removed JobPreviewDialog, added formId/dirty props, removed triage submit button)
- components/requests/request-edit-form.tsx (removed submit button, added formId/dirty/submitting props)
- components/requests/request-columns.tsx (simplified to single View button)
- components/requests/request-table.tsx (wired modal with requestIds/onNavigate)
- app/(dashboard)/requests/page.tsx (added searchParams for ?view= permalink)

The user's original vision: all table list views should use this modal pattern. After Requests is verified, the same pattern should be applied to Jobs, Assets, Templates, and Schedules as separate tasks.
</context>

<next_action>
Resume by asking the user to verify the latest changes on localhost:3000. Key things to test:
1. Split layout (details left, timeline right) — no tabs
2. "Update Request" button appears in bottom bar only when fields are changed
3. For GA Lead/Admin: changing category/priority/PIC and clicking "Update Request" saves AND triages
4. For requester: editing description/location and clicking "Update Request" saves changes
5. Photo lightbox ESC only closes lightbox, not the main modal
6. Prev/next navigation between requests
7. Linked jobs open in new tab

After verification, complete the quick workflow steps 6.5-8 (verifier, summary, state update, final commit).
</next_action>
