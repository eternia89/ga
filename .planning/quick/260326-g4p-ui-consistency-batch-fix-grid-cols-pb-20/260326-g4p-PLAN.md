---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(dashboard)/inventory/[id]/page.tsx
  - components/notifications/notification-dropdown.tsx
  - components/audit-trail/audit-trail-columns.tsx
  - components/maintenance/schedule-columns.tsx
  - components/maintenance/template-columns.tsx
  - app/(auth)/login/page.tsx
  - app/(auth)/reset-password/page.tsx
autonomous: true
requirements: [UI-CONSISTENCY]

must_haves:
  truths:
    - "Asset detail page has enough bottom padding so the sticky save bar does not overlap content"
    - "All link hover colors use hover:text-blue-700 consistently across the app"
  artifacts:
    - path: "app/(dashboard)/inventory/[id]/page.tsx"
      provides: "Asset detail page with pb-20 padding"
      contains: "pb-20"
    - path: "components/notifications/notification-dropdown.tsx"
      provides: "Notification dropdown with standardized hover color"
      contains: "hover:text-blue-700"
    - path: "components/audit-trail/audit-trail-columns.tsx"
      provides: "Audit trail record link with standardized hover color"
      contains: "hover:text-blue-700"
    - path: "components/maintenance/schedule-columns.tsx"
      provides: "Schedule name link with standardized hover color"
      contains: "hover:text-blue-700"
    - path: "components/maintenance/template-columns.tsx"
      provides: "Template name link with standardized hover color"
      contains: "hover:text-blue-700"
    - path: "app/(auth)/login/page.tsx"
      provides: "Login page forgot password link with standardized hover color"
      contains: "hover:text-blue-700"
    - path: "app/(auth)/reset-password/page.tsx"
      provides: "Reset password back-to-login link with standardized hover color"
      contains: "hover:text-blue-700"
  key_links: []
---

<objective>
Fix two UI consistency issues across the app: (1) add missing pb-20 bottom padding to the asset detail page so the sticky save bar does not overlap content, and (2) standardize all link hover colors to hover:text-blue-700, replacing the 5 instances of hover:text-blue-800 and 2 instances of hover:text-blue-500.

Purpose: Ensure visual consistency across all detail pages and interactive links.
Output: 7 files updated with consistent CSS classes.

NOTE: grid-cols-2 in schedule-detail.tsx and template-detail.tsx are INTENTIONALLY used for field-pair layouts (equal-width columns for Name+Category, Template+Interval). They are NOT the page-level content+timeline grid. Do NOT change these.
</objective>

<execution_context>
@/Users/melfice/code/ga/.claude/get-shit-done/workflows/execute-plan.md
@/Users/melfice/code/ga/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260326-g4p-ui-consistency-batch-fix-grid-cols-pb-20/260326-g4p-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pb-20 to asset detail page</name>
  <files>app/(dashboard)/inventory/[id]/page.tsx</files>
  <action>
On line 218, change:
```
className="space-y-6 py-6"
```
to:
```
className="space-y-6 py-6 pb-20"
```

This matches the pattern used by all other detail pages:
- requests/[id]/page.tsx:384 — `space-y-6 py-6 pb-20`
- jobs/[id]/page.tsx:451 — `space-y-6 py-6 pb-20`
- maintenance/schedules/[id]/page.tsx:122 — `space-y-6 py-6 pb-20`
- maintenance/templates/[id]/page.tsx:78 — `space-y-6 py-6 pb-20`

This is the only detail page missing it.
  </action>
  <verify>
    <automated>grep -n "pb-20" app/\(dashboard\)/inventory/\[id\]/page.tsx | head -5</automated>
  </verify>
  <done>Asset detail page container div includes pb-20 class, matching all other detail pages.</done>
</task>

<task type="auto">
  <name>Task 2: Standardize link hover colors to hover:text-blue-700</name>
  <files>
    components/notifications/notification-dropdown.tsx,
    components/audit-trail/audit-trail-columns.tsx,
    components/maintenance/schedule-columns.tsx,
    components/maintenance/template-columns.tsx,
    app/(auth)/login/page.tsx,
    app/(auth)/reset-password/page.tsx
  </files>
  <action>
Replace hover:text-blue-800 with hover:text-blue-700 in these 5 locations:

1. `components/notifications/notification-dropdown.tsx` line 34:
   `hover:text-blue-800` -> `hover:text-blue-700` (Mark all read button)

2. `components/notifications/notification-dropdown.tsx` line 64:
   `hover:text-blue-800` -> `hover:text-blue-700` (View all notifications link)

3. `components/audit-trail/audit-trail-columns.tsx` line 156:
   `hover:text-blue-800` -> `hover:text-blue-700` (Record ID link)

4. `components/maintenance/schedule-columns.tsx` line 31:
   `hover:text-blue-800` -> `hover:text-blue-700` (Schedule name button)

5. `components/maintenance/template-columns.tsx` line 26:
   `hover:text-blue-800` -> `hover:text-blue-700` (Template name button)

Replace hover:text-blue-500 with hover:text-blue-700 in these 2 locations:

6. `app/(auth)/login/page.tsx` line 282:
   `hover:text-blue-500` -> `hover:text-blue-700` (Forgot password link)

7. `app/(auth)/reset-password/page.tsx` line 116:
   `hover:text-blue-500` -> `hover:text-blue-700` (Back to login link)

After making all changes, run a codebase-wide check to confirm no remaining hover:text-blue-800 or hover:text-blue-500 instances exist in .tsx files (per CLAUDE.md bug fix protocol: scan entire codebase for same pattern).
  </action>
  <verify>
    <automated>echo "=== Should find 0 hover:text-blue-800 ===" && grep -rn "hover:text-blue-800" --include="*.tsx" app/ components/ | wc -l && echo "=== Should find 0 hover:text-blue-500 ===" && grep -rn "hover:text-blue-500" --include="*.tsx" app/ components/ | wc -l && echo "=== Should find 7 hover:text-blue-700 ===" && grep -rn "hover:text-blue-700" --include="*.tsx" app/ components/ | wc -l</automated>
  </verify>
  <done>All 7 link hover colors standardized to hover:text-blue-700. Zero instances of hover:text-blue-800 or hover:text-blue-500 remain in .tsx files.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes (no compile errors from class changes)
2. Zero instances of `hover:text-blue-800` in .tsx files
3. Zero instances of `hover:text-blue-500` in .tsx files
4. Asset detail page contains `pb-20` class
</verification>

<success_criteria>
- Asset detail page has pb-20 matching all other detail pages
- All 7 link hover colors use hover:text-blue-700
- No stray hover:text-blue-800 or hover:text-blue-500 remain
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/quick/260326-g4p-ui-consistency-batch-fix-grid-cols-pb-20/260326-g4p-SUMMARY.md`
</output>
